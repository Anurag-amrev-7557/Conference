---
phase: 12-structured-data-semantic-html
plan: 04
type: execute
wave: 4
depends_on:
  - 12-03
files_modified:
  - src/components/sections/HeroSection.tsx
  - src/pages/BlogPostPage.tsx
  - src/pages/BlogPage.tsx
  - src/pages/EventsPage.tsx
  - src/pages/LandingPage.tsx
  - scripts/verify-phase12-schema.mjs
requirements:
  - SCHEMA-06
autonomous: true
must_haves:
  truths:
    - "Landing has exactly one h1 in hero"
    - "Blog post author/thumbnail images have descriptive alt text"
    - "Public marketing pages have main landmark and single h1"
  artifacts:
    - path: scripts/verify-phase12-schema.mjs
      provides: Playwright JSON-LD + semantic smoke checks
---

<objective>
Semantic HTML audit fixes and automated verification script for Phase 12.

Purpose: SCHEMA-06 (D-20–D-23).
Output: h1/landmark/alt fixes + verify-phase12-schema.mjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/BOOK-12-structured-data-semantic-html/12-CONTEXT.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Semantic audit fixes</name>
  <files>src/components/sections/HeroSection.tsx, src/pages/BlogPostPage.tsx, src/pages/BlogPage.tsx, src/pages/EventsPage.tsx</files>
  <action>
    HeroSection: wrap visible headline in single h1.
    BlogPostPage: alt={article.title} on thumbnail; alt={article.authorName} on avatars.
    EventsPage: alt on event thumbnails from event.title.
    Ensure one h1 per audited page; preserve existing main/header/footer from Navbar/Footer.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Automated schema verification script</name>
  <files>scripts/verify-phase12-schema.mjs</files>
  <action>
    Playwright script: parse application/ld+json on /, /blog/slug, /events;
    assert @graph contains WebSite, BlogPosting, Event types as appropriate;
    assert one h1 on each route; assert key images have non-empty alt.
  </action>
  <verify>
    <automated>node scripts/verify-phase12-schema.mjs</automated>
  </verify>
</task>

</tasks>
