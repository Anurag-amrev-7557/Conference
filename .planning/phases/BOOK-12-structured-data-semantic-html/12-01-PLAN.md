---
phase: 12-structured-data-semantic-html
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/prisma/schema.prisma
  - server/prisma/migrations/*
  - server/src/schemas/content.ts
  - server/src/schemas/event.ts
  - src/lib/websiteData.ts
  - src/components/admin/SettingsManager.tsx
  - src/components/admin/EventManager.tsx
requirements:
  - SCHEMA-02
  - SCHEMA-05
autonomous: true
must_haves:
  truths:
    - "settings.book persists via admin PATCH and returns on GET /content/site"
    - "Event startDate/endDate persist via admin API and appear in client Event type"
    - "Events without startDate remain valid in UI but are excluded from schema later"
  artifacts:
    - path: src/lib/websiteData.ts
      provides: SiteSettings.book + AppEvent ISO date fields
    - path: server/prisma/schema.prisma
      provides: Event.startDate, Event.endDate optional DateTime
---

<objective>
Add CMS book metadata and Event ISO date columns — data foundation for JSON-LD builders.

Purpose: SCHEMA-02 Book fields and SCHEMA-05 machine-readable event dates (D-01, D-11).
Output: Prisma migration, types, Zod validation, admin UI for book + event dates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/BOOK-12-structured-data-semantic-html/12-CONTEXT.md
@.planning/phases/BOOK-12-structured-data-semantic-html/12-RESEARCH.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma Event startDate/endDate migration</name>
  <files>server/prisma/schema.prisma, server/prisma/migrations/</files>
  <action>
    Add optional `startDate DateTime?` and `endDate DateTime?` to Event model.
    Run `npx prisma migrate dev --name add_event_iso_dates` in server/.
    Update server event Zod schemas and routes to accept/return ISO strings or DateTime.
  </action>
  <verify>
    <automated>cd server && npx prisma validate && npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: settings.book type and Zod</name>
  <files>src/lib/websiteData.ts, server/src/schemas/content.ts</files>
  <action>
    Add `book?: { title?, authorName?, authorUrl?, isbn?, coverImageUrl?, publisherName?, publisherUrl? }` to SiteSettings.
    Add Zod refinement for settings.book with https URL refine on coverImageUrl/authorUrl/publisherUrl when set.
    Merge book in WebsiteDataProvider fetch like other settings fields.
  </action>
  <verify>
    <automated>cd server && npm run build && cd .. && npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Admin Book + Event date UI</name>
  <files>src/components/admin/SettingsManager.tsx, src/components/admin/EventManager.tsx</files>
  <action>
    SettingsManager: Book subsection with fields per D-02 and help copy (ISBN-13 hint).
    EventManager: datetime-local or ISO inputs for startDate/endDate on create/edit; map to API payload.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

</tasks>
