---
phase: 12-structured-data-semantic-html
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - package.json
  - package-lock.json
  - src/seo/JsonLd.tsx
  - src/seo/jsonLdConfig.ts
  - src/seo/usePageJsonLd.ts
  - src/seo/types.ts
  - src/seo/jsonLdConfig.test.ts
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03
  - SCHEMA-04
  - SCHEMA-05
autonomous: true
must_haves:
  truths:
    - "Pure resolvePageJsonLd returns @graph nodes per route without React"
    - "JsonLd renders single application/ld+json script via Helmet"
    - "Book node omitted when all book fields empty"
    - "BlogPosting omitted for noindex/unpublished articles"
  artifacts:
    - path: src/seo/jsonLdConfig.ts
      provides: buildWebSite, Organization, Book, BlogPosting, Event, Breadcrumb builders
    - path: src/seo/JsonLd.tsx
      provides: Helmet JSON-LD script emitter
---

<objective>
Implement typed JSON-LD builders and JsonLd presenter in src/seo/.

Purpose: SCHEMA-01–05 resolver layer (D-05–D-08, D-15–D-18).
Output: schema-dts, jsonLdConfig.ts, JsonLd.tsx, usePageJsonLd, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/BOOK-12-structured-data-semantic-html/12-01-SUMMARY.md
@.planning/phases/BOOK-12-structured-data-semantic-html/12-CONTEXT.md
@src/seo/seoConfig.ts
@src/seo/siteUrl.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Install schema-dts and JsonLd presenter</name>
  <files>package.json, src/seo/JsonLd.tsx, src/seo/types.ts</files>
  <action>
    npm install -D schema-dts@2.0.0
    JsonLd.tsx: Helmet child with one script type application/ld+json; content is JSON.stringify({ @context, @graph }).
    Export PageJsonLdGraph type from types.ts.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Pure jsonLdConfig builders</name>
  <files>src/seo/jsonLdConfig.ts, src/seo/usePageJsonLd.ts</files>
  <action>
    Implement resolvePageJsonLd({ pathname, data, article?, events? }):
    - / : WebSite + Organization + optional Book
    - /blog/:slug published indexable: BlogPosting + BreadcrumbList (3 items)
    - /blog : optional BreadcrumbList Home > Blog
    - /events : BreadcrumbList Home > Events + Event per published event with startDate
    All URLs via absoluteUrl. Use schema-dts WithContext types.
  </action>
  <verify>
    <automated>npm test -- src/seo/jsonLdConfig.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Unit tests for JSON-LD branches</name>
  <files>src/seo/jsonLdConfig.test.ts</files>
  <action>
    Tests: landing graph includes WebSite+Organization; Book when isbn set; Book absent when empty;
    BlogPosting on article; no BlogPosting when noindex; Event only with startDate; breadcrumb item counts.
    beforeEach setSiteOrigin('https://example.com').
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
</task>

</tasks>
