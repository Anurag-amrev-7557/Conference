---
phase: 12-structured-data-semantic-html
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - src/pages/LandingPage.tsx
  - src/pages/BlogPostPage.tsx
  - src/pages/BlogPage.tsx
  - src/pages/EventsPage.tsx
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03
  - SCHEMA-04
  - SCHEMA-05
autonomous: true
must_haves:
  truths:
    - "Landing page DOM includes JSON-LD script with WebSite and Organization after load"
    - "Blog post includes BlogPosting + BreadcrumbList in JSON-LD"
    - "Events page includes Event nodes for events with startDate"
  artifacts:
    - path: src/pages/LandingPage.tsx
      provides: SeoHead + JsonLd wired
    - path: src/pages/BlogPostPage.tsx
      provides: JsonLd with article graph
---

<objective>
Wire JsonLd on all public marketing pages that emit structured data.

Purpose: Live SCHEMA-01–05 in browser (D-09).
Output: usePageJsonLd on Landing, Blog, BlogPost, Events pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/BOOK-12-structured-data-semantic-html/12-02-SUMMARY.md
@src/seo/usePageJsonLd.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Wire JsonLd on landing and blog routes</name>
  <files>src/pages/LandingPage.tsx, src/pages/BlogPage.tsx, src/pages/BlogPostPage.tsx</files>
  <action>
    Import JsonLd + usePageJsonLd. Place JsonLd immediately after SeoHead.
    BlogPostPage: pass article to hook; respect loading gate from Phase 11.
    BlogPage: breadcrumb-only graph.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Wire JsonLd on events page</name>
  <files>src/pages/EventsPage.tsx</files>
  <action>
    usePageJsonLd with events from useWebsiteData; filter published events with startDate in resolver.
    JsonLd after SeoHead.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

</tasks>
