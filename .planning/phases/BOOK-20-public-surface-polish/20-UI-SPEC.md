---
phase: 20
slug: BOOK-20-public-surface-polish
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-19
scope: /blog index only (excludes /blog/:slug, events, community, 404)
---

# Phase 20 — UI Design Contract: Blog Index (`/blog`)

> Visual and interaction contract for the blog index page. Generated by gsd-ui-researcher from `20-CONTEXT.md`, Phase 17 bar, and `src/index.css` tokens. Verified by gsd-ui-checker.

**Quality bar:** Match Phase 17 landing **editorial premium** — `editorial-*`, `playbook-*`, `section-eyebrow`, `section-inner` rhythm, frosted cards, restrained motion, light theme only.

**Requirements in scope:** PAGE-02 (index slice), TYPE-03 (section rhythm), TYPE-04 (vertical rhythm), IMG-01 (featured + card images), PERF-01 (LCP-safe above-fold), UX-04 (blog polish), UX-06 (touch targets).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (no `components.json`; Tailwind v4 + `@utility` / component CSS in `index.css`) |
| Preset | not applicable |
| Component library | Radix (modals elsewhere only; blog index uses native form controls) |
| Icon library | lucide-react (`Search`, `ArrowRight`, optional `BookOpen` for empty state) |
| Font | **Instrument Serif** (`--font-serif`) display/headings; **Plus Jakarta Sans** (`--font-sans`) UI, meta, filters, search, newsletter |

---

## Spacing Scale

Declared values (multiples of 4; maps to `--space-*` in `@theme`):

| Token | Value | CSS variable | Usage on `/blog` |
|-------|-------|--------------|------------------|
| xs | 4px | `--space-1` | Meta dot gaps, icon nudge |
| sm | 8px | `--space-2` | Chip internal gap, filter row gap |
| md | 16px | `--space-4` | Card padding base, grid gap mobile |
| lg | 24px | `--space-6` | Header → featured gap (mobile), filter toolbar padding |
| xl | 32px | `--space-8` | Section vertical rhythm between major blocks |
| 2xl | 48px | `--space-12` | Page header bottom margin (`mb-12`), grid → newsletter |
| 3xl | 64px | `--space-16` | Featured bottom margin desktop (`mb-16`–`mb-20`) |

**Page shell horizontal padding (match playbook / Final CTA):**

| Breakpoint | Horizontal padding |
|------------|-------------------|
| default | `px-5` (20px) |
| `sm` | `px-8` (32px) |
| `lg` | `px-12` (48px) |
| `xl` | `px-16` (64px) |
| `2xl` | `px-16` (64px / `--space-16`) |

**Exceptions:**

| Element | Value | Notes |
|---------|-------|-------|
| Filter pill buttons | 44px min-height | `min-h-11` + horizontal `px-6` (B-12, UX-06) — touch target, not spacing scale |
| Search input | 44px min-height | `min-h-11`, full-width on `<sm` |
| Newsletter submit | 52px (`min-h-[3.25rem]`) | Reuse `btn-cta-primary`; ≥44px touch |
| Navbar offset | `pt-28`–`pt-32` | Align with `scroll-padding-top: 5.5rem` on `html` |
| Featured bottom margin | `mb-16` (64px) | Use `3xl` token only — no `mb-20` |

---

## Typography

**Four sizes only** (checker max). **Two weights only:** `400` (body + serif display), `600` (labels, chips, meta, CTAs).

| Role | Size | Weight | Line height | Font | Class / utility |
|------|------|--------|-------------|------|-----------------|
| Body | 16px (`1rem` / `--text-base`) | 400 | 1.65 | Plus Jakarta | `editorial-lede`, body default |
| Label | 11px | 600 | 1.4 | Plus Jakarta | `section-eyebrow`, `.blog-filter-pill`, `.blog-featured__badge` (all `text-[11px] font-semibold tracking-widest uppercase`) |
| Heading | `clamp(36px, 6vw, 60px)` | 400 italic | 1.08 | Instrument Serif | `editorial-heading editorial-heading--section` for page `h1` |
| Featured title | `clamp(28px, 4vw, 48px)` | 400 | 1.12 | Instrument Serif | `.blog-featured__title` — `h2`, not page `h1` |

**Page title (`h1`) copy structure:**  
`The` + `Agentic AI` (`.editorial-accent`, not italic) + `Playbook` — serif italic on outer words per screenshots; accent word uses `font-style: normal; color: var(--color-accent)`.

**Do not use:** `text-[10px]`, `font-light` (300), `font-bold` (700), third display face; sans-only blog headline (landing hero is sans-only; **blog keeps serif display** per B-02).

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#EBEBE8` (`--color-off`) | Page background `bg-off` |
| Secondary (30%) | `#FFFFFF` / `rgba(255,255,255,0.78)` | Filter inactive pills, search field, frosted grid cards (`playbook-article-card__link`), nav glass |
| Accent (10%) | `#003E99` (`--color-accent`) | See reserved list below |
| Accent hover / deep | `#002D70` (`--color-accent2`) | Subscribe hover, featured badge depth |
| Destructive | `#b42318` (waitlist error) | Form validation only; no destructive actions on index |

**Accent reserved for (only these elements):**

1. `section-eyebrow` text (“Dispatch”)
2. `h1` accent span (“Agentic AI”)
3. Active filter pill background
4. “Featured” badge on hero card
5. Category label on grid cards (`playbook-article-card__category`)
6. Card title / CTA hover color
7. Newsletter eyebrow (“Stay Ahead”) and primary subscribe button
8. Search field `focus` ring and icon `group-focus-within` color
9. Inline “Reset filters” text button on empty filter results

**Dark featured shell (not part of 60/30/10 — immersive media block):**

| Token | Value | Usage |
|-------|-------|-------|
| Featured surface | `#0a0a0a` → gradient overlay | `.blog-featured` container; image + `linear-gradient(to top, rgba(0,0,0,0.82), transparent 45%)` |
| On-image text | `#FFFFFF` / `rgba(255,255,255,0.6)` | Title, meta, author |

### Visual hierarchy

| Level | Element | When |
|-------|---------|------|
| Primary | Featured hero (`blog-featured`) | When a published article exists — image + `h2` title anchor attention |
| Primary (fallback) | Page `h1` + lede | When no featured article (zero posts) |
| Secondary | Filter toolbar + search | Discovery controls; active pill uses accent |
| Tertiary | Article grid (`playbook-article-card`) | Main library content |
| Quaternary | Newsletter (`blog-newsletter`) | Below grid; conversion without blocking browse |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (newsletter) | **Subscribe Now** |
| Secondary CTA (zero articles) | **Browse the playbook** → `/` (`#dispatch`) or **View community** → `/community` (executor picks one primary; prefer `/#dispatch`) |
| Empty state heading (zero published articles) | **Playbooks are on the way** |
| Empty state body | **We’re drafting new agentic AI guides. Check back soon—or explore the rest of the site.** |
| Filter empty heading | **No articles match** |
| Filter empty body | **Try another category or clear your search to see everything in the library.** |
| Filter reset control | **Show all articles** (button, accent text, `aria-label="Reset filters and search"`) |
| Error state (newsletter submit) | **Something went wrong.** + **Try again in a moment, or email us from the footer.** (reuse `waitlist-form__error` pattern) |
| Destructive confirmation | none on blog index |

**Static marketing copy (locked unless CMS adds blog page settings later):**

| Block | Copy |
|-------|------|
| Eyebrow | **Dispatch** |
| Page `h1` | **The** *Agentic AI* **Playbook** (accent on middle phrase) |
| Lede | **Diving deep into the architecture of agentic systems, no-code automation, and the future of business intelligence.** |
| Search placeholder | **Search resources…** |
| Newsletter eyebrow | **Stay Ahead** |
| Newsletter headline | **Get the latest** *intelligence* **delivered.** (italic on “intelligence”) |
| Newsletter lede | **Join 2,500+ builders and business owners receiving our weekly automation playbooks on Agentic AI.** |
| Email placeholder | **Enter your email** |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| — | — | shadcn not initialized; no third-party registry blocks |

---

## Scope & Exclusions

| In scope | Out of scope |
|----------|--------------|
| `/blog` index layout, header, featured, filters, search, grid, newsletter, empty states | `/blog/:slug` prose, related posts |
| Light theme polish | Dark theme, theme toggle |
| Reuse landing CSS families | Phase 19 Button primitive refactor (use existing `btn-cta-*`) |
| Client-side category + search filter | Server-side search API |
| Remove Framer above-fold; optional IO below-fold | Events, community, 404 |

---

## Page Structure

```
<body> (bg white global; page overrides)
└─ div.blog-page.min-h-screen.bg-off.overflow-x-hidden   [NO motion.div wrapper]
   ├─ Navbar (global, unchanged Phase 17)
   └─ main.blog-page__main
      │  pt-28 sm:pt-32 pb-16 md:pb-24
      │  max-w-[1600px] mx-auto px-5 sm:px-8 lg:px-12 xl:px-16 2xl:px-16
      │
      ├─ header.blog-page__header (text-center, mb-12 sm:mb-16)
      │  ├─ div.editorial-eyebrow.editorial-eyebrow--center
      │  │  ├─ span.editorial-eyebrow__rule
      │  │  ├─ span.section-eyebrow !mb-0  → "Dispatch"
      │  │  └─ span.editorial-eyebrow__rule
      │  ├─ h1.editorial-heading.editorial-heading--section
      │  └─ p.editorial-lede.max-w-2xl.mx-auto
      │
      ├─ section.blog-featured[aria-labelledby="blog-featured-heading"]  (if featuredArticle)
      │  └─ a.blog-featured__link → /blog/:slug
      │     ├─ div.blog-featured__media (aspect 21/9, radius --radius-lg 32px)
      │     │  └─ img [eager, fetchpriority=high, width/height]
      │     └─ div.blog-featured__content
      │        ├─ div badges (FEATURED + read time)
      │        ├─ h2#blog-featured-heading.blog-featured__title
      │        └─ author row (avatar alt = author name)
      │
      ├─ section.blog-library[aria-label="Article library"]
      │  ├─ div.blog-toolbar (flex col sm:row, gap-8, mb-12 sm:mb-16)
      │  │  ├─ div.blog-filters[role="group" aria-label="Categories"]
      │  │  │  └─ button.blog-filter-pill × N
      │  │  └─ div.blog-search
      │  │     ├─ label.visually-hidden → "Search articles"
      │  │     └─ input.blog-search__input
      │  │
      │  ├─ [conditional] div.blog-empty-filter → message + reset
      │  ├─ ul.playbook-articles.playbook-articles--trio (list-style none)
      │  │  └─ li.playbook-article-card × articles
      │  └─ [conditional] div.blog-empty-page → zero articles state
      │
      └─ section.blog-newsletter (below grid, mt-12 sm:mt-16)
         └─ div.blog-newsletter__shell (dark editorial card)
            ├─ header (eyebrow + h2 + lede)
            └─ form (email + Subscribe Now) OR WaitlistForm adapter
   └─ Footer
```

**Content order (locked B-06):** header → featured → toolbar → grid → newsletter → footer. Newsletter must not appear above the article grid.

---

## Component Class Mapping

| UI block | Primary classes | Source file | Notes |
|----------|-----------------|-------------|-------|
| Page wrapper | `blog-page`, `bg-off` | `BlogPage.tsx` + new `index.css` | Replace `motion.div` with plain `motionless` div |
| Main container | `blog-page__main` + landing horizontal padding | same | Replace `max-w-7xl` with `max-w-[1600px]` |
| Page header | `editorial-eyebrow`, `editorial-heading--section`, `editorial-lede` | existing `index.css` | **No** `initial={{ opacity: 0 }}` |
| Featured hero | `blog-featured`, `blog-featured__media`, `blog-featured__content`, `blog-featured__title` | **new** in `index.css` | Dark immersive card; `rounded-[32px]` (`--radius-lg`); aspect-ratio `21/9` |
| Featured badge | `blog-featured__badge` | new | `bg-accent text-white text-[11px] font-semibold uppercase tracking-widest rounded-full px-4 py-1.5` |
| Filter pills | `blog-filter-pill`, `blog-filter-pill--active` | new | Active: `bg-accent text-white shadow`; inactive: `bg-white border border-border/50 text-muted` |
| Search | `blog-search`, `blog-search__input` | new | Mirror white pill; `min-h-11 pl-12`; focus ring accent |
| Grid | `playbook-articles playbook-articles--trio`, `playbook-article-card` | `BlogSection.tsx` / `index.css` | Same markup pattern as landing; `--article-i` for stagger |
| Grid visibility | `playbook-section--visible` on parent OR duplicate reduced-motion-safe IO on `blog-library` | `BlogPage.tsx` | IntersectionObserver on `section.blog-library`, threshold 0.06 |
| Newsletter | `blog-newsletter`, `blog-newsletter__shell` | new + patterns from `final-cta-section` / screenshot | Dark shell: `bg-[#18181b]` (`zinc-900`), `rounded-[40px]`, accent glow `blog-newsletter__glow` |
| Newsletter CTA | `btn-cta-primary` with accent override OR dedicated `blog-newsletter__submit` | `btn-cta-primary` base | `min-h-[3.25rem]`, `bg-accent hover:bg-accent2` |
| Empty (page) | `blog-empty`, `editorial-lede` | new | Centered; link `btn-cta-secondary` |
| Empty (filter) | `blog-empty-filter` | new | Inline above grid |

### New CSS block requirements (`index.css`)

Add a `/* ===== BLOG INDEX ===== */` section defining at minimum:

```css
.blog-featured__link { display: block; border-radius: var(--radius-lg); overflow: hidden; }
.blog-featured__media { aspect-ratio: 21 / 9; background: #0a0a0a; }
.blog-featured__content { position: absolute; inset: auto 0 0 0; padding: 2rem 2.5rem; }
.blog-filter-pill { min-height: 44px; padding: 0 1.5rem; border-radius: var(--radius-full); }
.blog-newsletter__shell { border-radius: 2.5rem; padding: 2rem 1.25rem; background: #18181b; }
```

Featured must not use flat `bg-white shadow-2xl` card from current `BlogPage.tsx`.

---

## Breakpoints

| Breakpoint | Width | Layout behavior |
|------------|-------|-----------------|
| default | 320px+ | Single column; stacked toolbar; grid 1 col; featured full width |
| `sm` (640px) | ≥640px | Toolbar row; search fixed `w-64`; featured padding `p-10` |
| `md` (768px) | ≥768px | Grid 2 columns (`playbook-articles--trio` md rule) |
| `lg` (1024px) | ≥1024px | Grid 3 columns; featured title max width ~48rem |
| `xl` (1280px) | ≥1280px | Shell max-width 1600px centered |
| `2xl` (1536px) | ≥1536px | Horizontal padding `2xl:px-16` (64px) |

**No horizontal scroll** at 320px (NAV-06 / B-11): filter pills `flex-wrap`, search `w-full sm:w-64`.

---

## Motion & LCP (PERF-01, B-08, B-09)

| Zone | Motion rule | LCP |
|------|-------------|-----|
| Page `h1`, lede, eyebrow | **Static at opacity 1** — no Framer, no CSS animation on first paint | h1 is secondary LCP candidate; must be visible immediately |
| Featured image + title | **Static at opacity 1**; allow `transform` hover on image only (`scale` with `transition` ≤1s) | Featured image is primary LCP candidate: `loading="eager"` `fetchPriority="high"` `width={1200}` `height={514}` |
| Filter/search | CSS `transition-colors` 200ms only | — |
| Article grid | IntersectionObserver adds `playbook-section--visible` **below fold**; stagger via existing `--article-i` delays | Grid images `loading="lazy"` |
| Newsletter | Optional IO fade-in **after** grid; shell starts `opacity: 1` if in viewport on load is unlikely — if IO used, `rootMargin: -60px`, no opacity-0 on first paint for any text in viewport | — |

**Remove from `BlogPage.tsx`:** all `framer-motion` imports and `motion.*` wrappers above the grid.

**`prefers-reduced-motion: reduce`:** inherit global `index.css` reset; force `playbook-article-card` and any `blog-newsletter` transitions to `opacity: 1; transform: none` (mirror `.playbook-section` reduced-motion block).

---

## Interaction & Behavior

| Feature | Behavior |
|---------|----------|
| Category filter | Toggle `selectedCategory`; filter `remainingArticles` by `category`; ALL shows all non-featured |
| Search | Client-side case-insensitive match on `title` + `excerpt` combined with category filter |
| Featured article | Always `articles[0]` (most recent published); excluded from grid list |
| Card click | Whole card is `<Link>`; arrow affordance decorative |
| Newsletter | Submit via existing `WaitlistForm` / marketing service if wired; else form `preventDefault` + inline success copy matching waitlist success |
| Keyboard | Filter pills: `role="button"` native `<button>`; roving focus not required; visible `focus-visible:ring-2 focus-visible:ring-accent/30 focus-visible:ring-offset-2` |

---

## Accessibility

| Requirement | Implementation |
|-------------|----------------|
| One `h1` | Page title only |
| Featured title | `h2` with `id="blog-featured-heading"` |
| Filter group | `role="group"` `aria-label="Categories"`; `aria-pressed={true}` on active pill |
| Search | `<label class="sr-only">Search articles</label>` tied to input `id` |
| Images | Featured + card `alt={article.title}`; author avatar `alt={authorName}` |
| Touch targets | Filters, search, subscribe ≥44px height |
| Focus | All interactive elements use `focus-visible` rings (accent at 30% opacity) |

---

## Imagery (IMG-01)

| Image | Aspect | Dimensions | Loading |
|-------|--------|------------|---------|
| Featured | 21:9 | `width={1200}` `height={514}` | `eager` + `fetchPriority="high"` |
| Grid card | 16:10 (playbook) | `width={640}` `height={400}` | `lazy` |
| Author avatars | 1:1 | `width={40}` `height={40}` featured; `32` grid | lazy |

Use `object-fit: cover` on all thumbnails; neutral placeholder `background: #f4f6f8` from `.playbook-article-card__media`.

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS (visual hierarchy section added)
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS (4 sizes, 2 weights; 11px unified labels)
- [x] Dimension 5 Spacing: PASS (80px removed; scale-only padding)
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-05-19 (revision 1 — typography/spacing fixes)

---

## UI-SPEC COMPLETE

**Phase:** 20 — Public Surface Polish (blog index slice)  
**Design System:** Tailwind v4 + custom utilities (no shadcn)

### Contract Summary
- **Spacing:** 4–64px scale; page shell `max-w-[1600px]` with responsive `px-5` → `2xl:px-20`
- **Typography:** 4 sizes (11px label, 16px body, clamp section `h1`, clamp featured `h2`); weights 400/600 only
- **Color:** 60% `#EBEBE8` off, 30% white/frosted cards, 10% `#003E99` accent on reserved elements only; dark featured shell separate
- **Copywriting:** 10 strings defined (CTA, dual empty states, error, static hero/newsletter)
- **Registry:** none

### File Created
`/Users/anuragverma/Downloads/book website-frontend/.planning/phases/BOOK-20-public-surface-polish/20-UI-SPEC.md`

### Pre-Populated From
| Source | Decisions Used |
|--------|----------------|
| `20-CONTEXT.md` | 14 (B-01–B-14) |
| `17-CONTEXT.md` | 5 (light-only, LCP, touch, spacing bar, no dark) |
| `REQUIREMENTS.md` | 6 (PAGE-02, TYPE-03/04, IMG-01, PERF-01, UX-04/06) |
| `index.css` | tokens, editorial + playbook + btn-cta + section-inner |
| `components.json` | no |
| Screenshots | layout confirmation (featured dark, pills, newsletter) |

### Ready for Verification
UI-SPEC complete. Checker can validate against 6 dimensions; planner/executor should refactor `BlogPage.tsx` per class map and motion rules.
