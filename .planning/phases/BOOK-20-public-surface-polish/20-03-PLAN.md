---
phase: BOOK-20-public-surface-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 20-02
files_modified:
  - src/index.css
  - src/pages/BlogPage.tsx
  - src/components/landing/WaitlistForm.tsx
  - scripts/verify-phase20-blog-index.mjs
  - package.json
autonomous: true
requirements:
  - PAGE-02
  - UX-04
  - UX-06
  - PERF-01
must_haves:
  truths:
    - Newsletter CTA below article grid reuses WaitlistForm with marketing telemetry
    - verify:phase20 script guards Framer removal and blog structure
  artifacts:
    - scripts/verify-phase20-blog-index.mjs
    - src/components/landing/WaitlistForm.tsx
    - src/pages/BlogPage.tsx
  key_links:
    - from: src/pages/BlogPage.tsx
      to: src/components/landing/WaitlistForm.tsx
      via: blog-newsletter section
---

<objective>
Add newsletter block below grid using extended WaitlistForm, dark blog-newsletter shell, and Phase 20 verification script.

Purpose: Complete PAGE-02 blog index slice with conversion CTA and regression guard (B-07, PERF-01).
Output: `npm run verify:phase20` passes; newsletter wired with locked UI-SPEC copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/BOOK-20-public-surface-polish/20-UI-SPEC.md
@.planning/phases/BOOK-20-public-surface-polish/20-RESEARCH.md
@src/components/landing/WaitlistForm.tsx
@src/components/sections/FinalCTA.tsx
@scripts/verify-phase17-nav-hero.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend WaitlistForm for blog variant</name>
  <files>src/components/landing/WaitlistForm.tsx</files>
  <read_first>src/components/landing/WaitlistForm.tsx</read_first>
  <read_first>.planning/phases/BOOK-20-public-surface-polish/20-UI-SPEC.md</read_first>
  <action>
Add optional props with defaults preserving FinalCTA behavior:
- `analyticsLocation?: string` default `'waitlist_form'`
- `variant?: 'light' | 'dark'` default `'light'`
- `showGuideLabel?: boolean` default true
- `submitLabel?: string` default 'Get the playbook'
- `placeholder?: string` default 'you@company.com'
- `successTitle?: string` default "You're on the list"
- `successCopy?: string` default existing copy

When variant is dark: apply classes `waitlist-form--dark` on root; use `blog-newsletter__input` class on input when passed via `inputClassName` prop OR map variant internally.

`MarketingService.logEvent('form_submit', { location: analyticsLocation })`.

Keep existing FinalCTA usage working without prop changes.
  </action>
  <verify>npm run build 2>&1 | tail -10</verify>
  <acceptance_criteria>
    - WaitlistForm accepts analyticsLocation and variant props
    - Default export behavior unchanged for FinalCTA (no required new props)
    - logEvent uses analyticsLocation variable
  </acceptance_criteria>
  <done>WaitlistForm supports blog newsletter variant</done>
</task>

<task type="auto">
  <name>Task 2: Blog newsletter section</name>
  <files>src/pages/BlogPage.tsx</files>
  <files>src/index.css</files>
  <read_first>src/pages/BlogPage.tsx</read_first>
  <read_first>src/components/sections/FinalCTA.tsx</read_first>
  <action>
After `section.blog-library`, add `section.blog-newsletter` with `mt-12 sm:mt-16` (not mt-40).

Shell: `.blog-newsletter__shell` dark zinc-900, rounded 2.5rem, accent glow element `.blog-newsletter__glow` (optional blur).

Copy per UI-SPEC:
- Eyebrow: Stay Ahead
- h2: Get the latest *intelligence* delivered. (italic on intelligence via span)
- Lede: Join 2,500+ builders...

Render WaitlistForm with variant="dark", analyticsLocation="blog_newsletter", showGuideLabel={false}, submitLabel="Subscribe Now", placeholder="Enter your email".

Complete `.blog-newsletter` and dark input overrides in index.css.
  </action>
  <verify>grep -q "blog-newsletter" src/pages/BlogPage.tsx && grep -q "WaitlistForm" src/pages/BlogPage.tsx</verify>
  <acceptance_criteria>
    - Newsletter section appears after blog-library in DOM order
    - WaitlistForm rendered with analyticsLocation blog_newsletter and submitLabel Subscribe Now
    - index.css contains blog-newsletter__shell styles
  </acceptance_criteria>
  <done>Newsletter below grid with wired submit</done>
</task>

<task type="auto">
  <name>Task 3: verify-phase20 script and npm script</name>
  <files>scripts/verify-phase20-blog-index.mjs</files>
  <files>package.json</files>
  <read_first>scripts/verify-phase17-nav-hero.mjs</read_first>
  <action>
Create `scripts/verify-phase20-blog-index.mjs` asserting:
- BlogPage.tsx: no `framer-motion` import
- BlogPage.tsx: contains `blog-page`, `blog-featured`, `playbook-articles--trio`, `blog-newsletter`
- BlogPage.tsx: contains `fetchPriority="high"` and no `initial={{ opacity: 0`
- index.css: contains `BLOG INDEX` and `blog-featured`

Add to package.json: `"verify:phase20": "node scripts/verify-phase20-blog-index.mjs"`
  </action>
  <verify>npm run verify:phase20 && npm run build</verify>
  <acceptance_criteria>
    - npm run verify:phase20 exits 0
    - package.json scripts includes verify:phase20
    - npm run build exits 0
  </acceptance_criteria>
  <done>Phase 20 blog index verification gate exists</done>
</task>

</tasks>

<verification>
- [ ] `npm run verify:phase20`
- [ ] `npm run build`
- [ ] `npm run lint` (if configured)
</verification>

<success_criteria>
- Blog index slice complete per UI-SPEC with automated verify script
</success_criteria>
