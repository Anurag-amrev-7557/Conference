---
phase: BOOK-20-public-surface-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/pages/BlogPage.tsx
autonomous: true
requirements:
  - PERF-01
  - IMG-01
  - TYPE-03
  - TYPE-04
  - PAGE-02
must_haves:
  truths:
    - Blog index above-fold content is visible at first paint with no Framer Motion
    - Featured article uses dark immersive shell per UI-SPEC
    - Page shell matches landing max-width and editorial header
  artifacts:
    - src/index.css
    - src/pages/BlogPage.tsx
  key_links:
    - from: src/pages/BlogPage.tsx
      to: .planning/phases/BOOK-20-public-surface-polish/20-UI-SPEC.md
      via: blog-page structure and blog-featured classes
---

<objective>
Add blog index CSS block, refactor BlogPage shell/header/featured, and remove all Framer Motion from BlogPage for LCP-safe above-fold rendering.

Purpose: Establish Phase 17–quality foundation before toolbar/grid/newsletter work.
Output: Static `blog-page` wrapper, editorial header, `blog-featured*` hero, no `framer-motion` in BlogPage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/BOOK-20-public-surface-polish/20-CONTEXT.md
@.planning/phases/BOOK-20-public-surface-polish/20-UI-SPEC.md
@.planning/phases/BOOK-20-public-surface-polish/20-RESEARCH.md
@src/components/sections/BlogSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BLOG INDEX CSS block</name>
  <files>src/index.css</files>
  <read_first>src/index.css</read_first>
  <read_first>.planning/phases/BOOK-20-public-surface-polish/20-UI-SPEC.md</read_first>
  <action>
Add section `/* ===== BLOG INDEX ===== */` after playbook section styles with minimum classes:
- `.blog-page .editorial-lede { font-weight: 400; }`
- `.blog-featured`, `.blog-featured__link`, `.blog-featured__media` (aspect-ratio 21/9, radius var(--radius-lg)), `.blog-featured__content`, `.blog-featured__title`, `.blog-featured__badge` (11px, uppercase, accent bg)
- Placeholder stubs for `.blog-filter-pill`, `.blog-search`, `.blog-newsletter`, `.blog-empty` (filled in plan 02/03)

Featured media: dark bg `#0a0a0a`, gradient overlay via pseudo or child overlay class.
  </action>
  <verify>grep -q "BLOG INDEX" src/index.css && grep -q "blog-featured" src/index.css</verify>
  <acceptance_criteria>
    - index.css contains `/* ===== BLOG INDEX ===== */`
    - index.css contains `.blog-featured__media` with aspect-ratio 21/9
    - `.blog-page .editorial-lede` sets font-weight 400
  </acceptance_criteria>
  <done>Blog CSS foundation exists</done>
</task>

<task type="auto">
  <name>Task 2: Refactor BlogPage shell and remove Framer</name>
  <files>src/pages/BlogPage.tsx</files>
  <read_first>src/pages/BlogPage.tsx</read_first>
  <read_first>src/components/sections/BlogSection.tsx</read_first>
  <action>
1. Remove `import { motion } from 'framer-motion'` and all `motion.*` elements.
2. Root: `div.blog-page.min-h-screen.bg-off.overflow-x-hidden`.
3. `main.blog-page__main` with `pt-28 sm:pt-32 pb-16 md:pb-24 max-w-[1600px] mx-auto px-5 sm:px-8 lg:px-12 xl:px-16 2xl:px-16`.
4. Replace header with editorial pattern from UI-SPEC: `editorial-eyebrow--center`, `section-eyebrow` "Dispatch", `h1.editorial-heading.editorial-heading--section` with `editorial-accent` on "Agentic AI", `p.editorial-lede`.
5. No `initial`/`animate`/`transition` props; no `opacity: 0` on h1 or header children.
6. Keep SeoHead, JsonLd, Navbar, Footer unchanged.
  </action>
  <verify>! grep -q "framer-motion" src/pages/BlogPage.tsx && npm run build 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - BlogPage.tsx does not import framer-motion
    - BlogPage.tsx contains `blog-page` and `max-w-[1600px]`
    - BlogPage.tsx contains `editorial-heading--section` and no `motion.`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Static blog shell with editorial header</done>
</task>

<task type="auto">
  <name>Task 3: Implement dark featured hero</name>
  <files>src/pages/BlogPage.tsx</files>
  <read_first>src/pages/BlogPage.tsx</read_first>
  <read_first>.planning/phases/BOOK-20-public-surface-polish/20-UI-SPEC.md</read_first>
  <action>
Replace white featured card with `section` + `a.blog-featured__link`:
- `featuredArticle = articles[0]`, `remainingArticles = articles.slice(1)` (keep logic)
- `h2#blog-featured-heading.blog-featured__title` (not h1)
- Badge "Featured" + read time using `blog-featured__badge` (11px)
- Image: width={1200} height={514} loading="eager" fetchPriority="high" alt={featuredArticle.title}
- Author avatar alt={featuredArticle.authorName} (fix empty alt)
- Remove flat `bg-white shadow-2xl` featured wrapper
  </action>
  <verify>grep -q "blog-featured" src/pages/BlogPage.tsx && grep -q 'fetchPriority="high"' src/pages/BlogPage.tsx</verify>
  <acceptance_criteria>
    - Featured block uses `blog-featured` classes
    - Featured title is h2 with id blog-featured-heading
    - Featured image has width 1200 height 514 loading eager fetchPriority high
    - Author img alt is authorName not empty string
  </acceptance_criteria>
  <done>Dark immersive featured hero on blog index</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passes
- [ ] `grep framer-motion src/pages/BlogPage.tsx` returns no matches
</verification>

<success_criteria>
- Blog index shell and featured match 20-UI-SPEC; Framer removed from BlogPage
</success_criteria>
