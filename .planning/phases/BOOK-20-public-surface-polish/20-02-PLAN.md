---
phase: BOOK-20-public-surface-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/index.css
  - src/pages/BlogPage.tsx
autonomous: true
requirements:
  - PAGE-02
  - UX-04
  - UX-06
must_haves:
  truths:
    - Visitor can filter articles by category and search title/excerpt on /blog
    - Article grid uses landing playbook cards below featured content
    - Empty states appear for zero articles and zero filter matches
  artifacts:
    - src/pages/BlogPage.tsx
    - src/index.css
  key_links:
    - from: src/pages/BlogPage.tsx
      to: src/components/sections/BlogSection.tsx
      via: playbook-articles--trio markup
---

<objective>
Wire filter toolbar, client-side search, playbook article grid with IntersectionObserver stagger, and empty states on the blog index.

Purpose: Deliver browseable library UX (B-05, B-06, B-13, B-14) before newsletter block.
Output: Functional toolbar + grid + empties; newsletter remains for plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/BOOK-20-public-surface-polish/20-CONTEXT.md
@.planning/phases/BOOK-20-public-surface-polish/20-UI-SPEC.md
@.planning/phases/BOOK-20-public-surface-polish/20-RESEARCH.md
@src/components/sections/BlogSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter pills and search toolbar CSS</name>
  <files>src/index.css</files>
  <read_first>src/index.css</read_first>
  <read_first>.planning/phases/BOOK-20-public-surface-polish/20-UI-SPEC.md</read_first>
  <action>
Complete styles for:
- `.blog-filter-pill` min-height 44px, rounded-full, inactive white border
- `.blog-filter-pill--active` bg accent text white
- `.blog-search__input` min-height 44px, pl-12, rounded-full, focus ring accent
- `.blog-empty`, `.blog-empty-filter` centered typography
- `focus-visible:ring-2 ring-accent/30 ring-offset-2` on pills and search
  </action>
  <verify>grep -q "blog-filter-pill" src/index.css && grep -q "blog-search" src/index.css</verify>
  <acceptance_criteria>
    - index.css defines blog-filter-pill and blog-filter-pill--active
    - index.css defines blog-search__input with min-height 44px equivalent
  </acceptance_criteria>
  <done>Toolbar CSS complete</done>
</task>

<task type="auto">
  <name>Task 2: Search and category filter state</name>
  <files>src/pages/BlogPage.tsx</files>
  <read_first>src/pages/BlogPage.tsx</read_first>
  <read_first>src/lib/websiteData.ts</read_first>
  <action>
Add `searchQuery` state and `useMemo` pipeline:
- Categories: ALL, RESEARCH, STRATEGY, PLAYBOOK, GUIDE
- `gridArticles` from `remainingArticles` filtered by category then case-insensitive title+excerpt
- Toolbar: `role="group" aria-label="Categories"` on filter container
- Each pill: `button.blog-filter-pill` with `aria-pressed={selectedCategory === cat}`
- Search: controlled input, `label.sr-only` "Search articles", placeholder "Search resources…", class `blog-search__input`, min-h-11
- Reset handler: setSelectedCategory('ALL'); setSearchQuery('')
  </action>
  <verify>grep -q "searchQuery" src/pages/BlogPage.tsx && grep -q "gridArticles" src/pages/BlogPage.tsx</verify>
  <acceptance_criteria>
    - BlogPage.tsx contains searchQuery state and gridArticles useMemo
    - Filter buttons use aria-pressed
    - Search input has sr-only label Search articles
  </acceptance_criteria>
  <done>Client-side filter pipeline wired</done>
</task>

<task type="auto">
  <name>Task 3: Playbook grid and empty states</name>
  <files>src/pages/BlogPage.tsx</files>
  <read_first>src/components/sections/BlogSection.tsx</read_first>
  <read_first>src/pages/BlogPage.tsx</read_first>
  <action>
1. Wrap library in `section.blog-library.playbook-section` with ref + IO (copy BlogSection observer) — add `playbook-section--visible` on intersect only on this section.
2. If `articles.length === 0`: render `div.blog-empty` with heading "Playbooks are on the way", body per UI-SPEC, link `btn-cta-secondary` to `/#dispatch` text "Browse the playbook".
3. Else if `gridArticles.length === 0`: render `blog-empty-filter` with "No articles match", reset button "Show all articles" calling reset handler, aria-label on reset.
4. Else: `ul.playbook-articles.playbook-articles--trio` with markup matching BlogSection (media 640x400 lazy, meta, title, excerpt, cta) — map `gridArticles`, set `--article-i`.
5. Content order: header → featured → blog-library (toolbar + grid/empty) — newsletter NOT in this plan.
6. Card img alt={article.title}; author avatars alt={authorName}.
  </action>
  <verify>grep -q "playbook-articles--trio" src/pages/BlogPage.tsx && grep -q "blog-empty" src/pages/BlogPage.tsx</verify>
  <acceptance_criteria>
    - Grid uses playbook-articles--trio and playbook-article-card classes
    - blog-library section has playbook-section class and useEffect IntersectionObserver
    - Zero articles shows blog-empty with Browse the playbook link to /#dispatch
    - Zero filter results shows blog-empty-filter and Show all articles control
  </acceptance_criteria>
  <done>Playbook grid and dual empty states live</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passes
- [ ] Manual: /blog filter pills and search narrow grid; reset restores all
</verification>

<success_criteria>
- Blog library is browsable with landing-quality cards and accessible toolbar
</success_criteria>
