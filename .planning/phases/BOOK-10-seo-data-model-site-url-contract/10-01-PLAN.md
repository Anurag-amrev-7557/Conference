---
phase: 10-seo-data-model-site-url-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/anuragverma/Downloads/book website-frontend/server/src/lib/siteUrl.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/index.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/contentRoutes.ts
  - /Users/anuragverma/Downloads/book website-frontend/src/seo/siteUrl.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/.env.example
  - /Users/anuragverma/Downloads/book website-frontend/.env.example
requirements:
  - CRAWL-01
autonomous: true
must_haves:
  truths:
    - "D-01 scope: SITE_URL is server-only; no VITE_SITE_URL introduced."
    - "CRAWL-01: Production boot fails if SITE_URL unset or invalid."
    - "CRAWL-01: GET /api/v1/content/site includes siteUrl string from SITE_URL."
    - "Client absoluteUrl() reads siteUrl from API payload, not hardcoded monograph host."
  artifacts:
    - path: /Users/anuragverma/Downloads/book website-frontend/server/src/lib/siteUrl.ts
      provides: getSiteUrl() and absoluteUrl(path)
    - path: /Users/anuragverma/Downloads/book website-frontend/src/seo/siteUrl.ts
      provides: Client URL helper fed by hydrated siteUrl
  key_links:
    - from: /Users/anuragverma/Downloads/book website-frontend/server/src/routes/contentRoutes.ts
      to: /Users/anuragverma/Downloads/book website-frontend/server/src/lib/siteUrl.ts
      via: siteUrl on fetchSitePayload response
      pattern: siteUrl
---

<objective>
Establish SITE_URL as the single authoritative public-site origin with shared server/client URL helpers.

Purpose: CRAWL-01 — eliminate hardcoded host drift before Phases 11–14 meta/sitemap/JSON-LD work.
Output: siteUrl.ts (server + client), siteUrl on content/site API, env documentation.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/BOOK-10-seo-data-model-site-url-contract/10-CONTEXT.md
@.planning/phases/BOOK-10-seo-data-model-site-url-contract/10-RESEARCH.md
@.planning/phases/BOOK-10-seo-data-model-site-url-contract/10-VALIDATION.md
</execution_context>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-10-01 | Open redirect via malformed SITE_URL | medium | Validate URL has https: scheme; strip trailing slash in getSiteUrl() |
| T-10-02 | Client env drift duplicates origin | medium | No VITE_SITE_URL; expose read-only siteUrl via API only |
</threat_model>

<context>
Mirror jwtSecret.ts production fail-fast pattern. Dev fallback: http://localhost:5173 with one-time warning.
index.html still has monograph host until Phase 11 — this plan only establishes the contract.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create server siteUrl module</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/src/lib/jwtSecret.ts
    - /Users/anuragverma/Downloads/book website-frontend/.planning/phases/BOOK-10-seo-data-model-site-url-contract/10-RESEARCH.md
  </read_first>
  <files>server/src/lib/siteUrl.ts</files>
  <acceptance_criteria>
    - siteUrl.ts exports getSiteUrl(): string and absoluteUrl(path: string): string.
    - getSiteUrl() strips trailing slash from origin.
    - NODE_ENV=production with missing SITE_URL throws Error containing SITE_URL.
    - NODE_ENV=development without SITE_URL returns http://localhost:5173 and logs warning once.
    - absoluteUrl('/blog/foo') returns `${origin}/blog/foo` with single slash join.
  </acceptance_criteria>
  <action>
    Create server/src/lib/siteUrl.ts following jwtSecret.ts structure. SITE_URL must be https URL in production. DEV_FALLBACK=http://localhost:5173. absoluteUrl normalizes path to start with /.
  </action>
</task>

<task type="auto">
  <name>Task 2: Boot-time validation and expose siteUrl on content/site</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/src/index.ts
    - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/contentRoutes.ts
    - /Users/anuragverma/Downloads/book website-frontend/server/src/lib/siteUrl.ts
  </read_first>
  <files>server/src/index.ts, server/src/routes/contentRoutes.ts</files>
  <acceptance_criteria>
    - index.ts calls getSiteUrl() after dotenv.config() before app.listen.
    - fetchSitePayload() return object includes siteUrl: getSiteUrl().
    - GET /api/v1/content/site JSON body contains siteUrl string key.
  </acceptance_criteria>
  <action>
    Import getSiteUrl in index.ts for early fail-fast. In contentRoutes.ts fetchSitePayload, add siteUrl field. Include siteUrl in bundled GET /content response if a combined endpoint exists.
  </action>
</task>

<task type="auto">
  <name>Task 3: Client seo/siteUrl helper and env examples</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/src/components/WebsiteDataProvider.tsx
    - /Users/anuragverma/Downloads/book website-frontend/server/.env.example
    - /Users/anuragverma/Downloads/book website-frontend/.env.example
  </read_first>
  <files>src/seo/siteUrl.ts, server/.env.example, .env.example</files>
  <acceptance_criteria>
    - src/seo/siteUrl.ts exports setSiteOrigin(url), getSiteOrigin(), absoluteUrl(path).
    - setSiteOrigin called from WebsiteDataProvider when remote site payload includes siteUrl.
    - server/.env.example documents SITE_URL=https://monograph.superhumanly.ai with comment (public site origin, not API host).
    - Root .env.example does NOT add VITE_SITE_URL.
  </acceptance_criteria>
  <action>
    Create src/seo/siteUrl.ts module-level origin store. Wire WebsiteDataProvider to call setSiteOrigin(data.siteUrl) after fetch. Document SITE_URL in server/.env.example only.
  </action>
</task>

</tasks>

<verification>
- [ ] `cd server && npm run build` exits 0
- [ ] `curl -s http://localhost:PORT/api/v1/content/site | jq .siteUrl` returns non-empty URL
- [ ] NODE_ENV=production SITE_URL= node dist/index.js fails at boot
</verification>
