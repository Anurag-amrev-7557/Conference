# Plan 17-01 — Theme runtime: next-themes, FOUC boot, provider shell

**Requirements:** DSM-02, DSM-03, DSM-04 (provider + persistence wiring)  
**Prereq:** None  
**Parallel:** Can start before heavy CSS; coordinate storage key with 17-03.

## Goal

Ship `next-themes`, blocking inline boot in `index.html`, `color-scheme` meta, and `ThemeProvider` at the React root so system dark and stored preference apply without a full flash of wrong theme, compatible with later CMS overrides.

## Tasks

1. **Dependency:** `npm install next-themes@^0.4.6`
2. **`index.html`:** Add `<meta name="color-scheme" content="light dark" />`. Add **inline** `<script>` before module script: parse `localStorage` (use same `storageKey` as `ThemeProvider`), respect `prefers-color-scheme` when theme is `system` or unset; add/remove `dark` class on `document.documentElement`. Keep script minimal and synchronous.
3. **`src/main.tsx`:** Wrap tree with `ThemeProvider` from `next-themes` (`attribute="class"`, `defaultTheme="system"`, `enableSystem`, explicit `storageKey`).
4. **Verify:** Cold load in OS dark — no long white flash; toggle OS theme with CMS default `system` (after 17-03) updates as expected.

## Verification

- [ ] `npm test` passes
- [ ] Manual: hard refresh in dark OS, documentElement has `dark` before React paint (DevTools throttle)
- [ ] Manual: set localStorage theme to `light`, OS dark — first paint light, then CMS `system` still coherent after 17-03

## Notes

- CMS override of inline guess is completed in **17-03** (`applyAppearance` + `setTheme`).
