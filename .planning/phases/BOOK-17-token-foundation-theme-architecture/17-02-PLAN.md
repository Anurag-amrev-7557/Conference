# Plan 17-02 — Semantic tokens & global CSS (light/dark)

**Requirements:** DSM-01, DSM-06, DSM-07  
**Prereq:** 17-01 merged (dark class on html works)  
**Depends on CONTEXT:** D-17-01..04, D-17-17..20

## Goal

Define semantic color tokens (cool light canvas, stepped `~#1C1C1E` dark, `#1D1D1F` primary text on light, hairline borders) in `src/index.css` `@theme` / `@layer` / `@variant` as appropriate for Tailwind v4; refactor **shared** `@utility` rules (glass, CTA shells, body) off hardcoded `#ffffff` / light-only rgba to semantic variables; enforce **one** above-fold `backdrop-filter` (document which layer wins in code comments).

## Tasks

1. **Token map:** Add semantic CSS variables for `--color-bg`, `--color-surface`, `--color-surface-elevated`, `--color-text`, `--color-text-muted`, `--color-border`, plus dark overrides when `.dark` on root (match CONTEXT numeric intent; use exact hex in `@theme` or `:root` + `.dark`).
2. **Register Tailwind theme keys** so utilities align (e.g. map `bg` to canvas, avoid breaking existing `text-text` if already present — migrate `bg-off` usage intentionally).
3. **`body` / html:** Replace `background: #ffffff` with semantic bg; ensure cool light default matches D-17-14.
4. **Glass utilities:** Update `glass-pill`, `glass-premium`, related nav/card utilities per D-17-17 (nav dark: lower blur + hairline), D-17-18 (cards: subtle frosted dark), D-17-19 (modal scrim tokens — pair with Radix overlay in later phase if variables only here).
5. **Shadow ladder:** Apple-minimal hairline-first elevation (D-17-04); reduce stacked heavy shadows in `@theme` where they fight dark mode.
6. **`App.tsx`:** Change root wrapper from `bg-off` to semantic `bg-bg` (or equivalent token class).
7. **Audit grep:** `rg "bg-white|#fff|#ffffff|rgba\\(255"` in `src/index.css` and fix occurrences in this file; do **not** sweep all `src/components` (Phase 19).

## Verification

- [ ] `npm run build:no-prerender` succeeds
- [ ] Toggle `.dark` on `<html>` in DevTools: surfaces, text, borders readable; nav glass matches D-17-17
- [ ] `rg "#ffffff"` in `index.css` — no unintended body/canvas hardcodes

## Out of scope

- Individual page sections — Phase 19.
