---
phase: 02-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/anuragverma/Downloads/book website-frontend/server/src/lib/jwtSecret.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/authRoutes.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/adminRoutes.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/index.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/seed.ts
  - /Users/anuragverma/Downloads/book website-frontend/.gitignore
  - /Users/anuragverma/Downloads/book website-frontend/.env.example
  - /Users/anuragverma/Downloads/book website-frontend/server/.env.example
requirements:
  - SEC-01
  - SEC-02
  - SEC-06
autonomous: true
must_haves:
  truths:
    - "D-01: Production boot fails if JWT_SECRET unset, empty, or supersecret."
    - "D-02: Development allows supersecret with one-time loud warning."
    - "D-03: Single jwtSecret module imported by authRoutes and adminRoutes."
    - "D-04: POST /api/v1/auth/login limited to 5 requests per 15 minutes per IP."
    - "D-05: Login 429 returns generic JSON, no user enumeration."
    - "D-06: No JWT refresh tokens added; 24h expiry and Phase 1 /admin/me gate unchanged."
    - "D-22: server/prisma/dev.db and *.db-journal in .gitignore."
    - "D-23: seed.ts upsert does not overwrite existing admin password on update."
    - "D-24: .env.example documents JWT_SECRET without VITE marketing key."
  artifacts:
    - path: /Users/anuragverma/Downloads/book website-frontend/server/src/lib/jwtSecret.ts
      provides: Centralized JWT secret resolution
  key_links:
    - from: /Users/anuragverma/Downloads/book website-frontend/server/src/routes/authRoutes.ts
      to: /Users/anuragverma/Downloads/book website-frontend/server/src/lib/jwtSecret.ts
      via: import getJwtSecret
      pattern: jwtSecret
---

<objective>
Harden JWT configuration, throttle login brute force, and fix secrets/seed/gitignore hygiene.

Purpose: SEC-01, SEC-02, SEC-06 — eliminate trivial forgery and credential reset risks before XSS/proxy work.
Output: Shared jwtSecret module, login rate limiter, safe seed, dev.db ignored.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/BOOK-02-security-hardening/02-CONTEXT.md
@.planning/phases/BOOK-02-security-hardening/02-RESEARCH.md
@.planning/phases/BOOK-02-security-hardening/02-VALIDATION.md
</execution_context>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-02-01 | Forged admin JWT via default secret | high | Fail-fast production boot (D-01) |
| T-02-02 | Login brute force | high | express-rate-limit 5/15min/IP (D-04) |
| T-02-03 | Committed SQLite dev.db leaks data | medium | .gitignore + git rm --cached (D-22) |
| T-02-04 | Seed resets production admin password | high | upsert update: {} for admin (D-23) |
</threat_model>

<context>
Community router already uses express-rate-limit (60/15min). Login limiter is separate, mounted only on authRoutes login POST.
Import jwtSecret at module load so production misconfig fails before listen().
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create jwtSecret module and wire auth + admin routes</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/authRoutes.ts
    - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/adminRoutes.ts
    - /Users/anuragverma/Downloads/book website-frontend/.planning/phases/BOOK-02-security-hardening/02-CONTEXT.md
  </read_first>
  <files>server/src/lib/jwtSecret.ts, server/src/routes/authRoutes.ts, server/src/routes/adminRoutes.ts</files>
  <acceptance_criteria>
    - jwtSecret.ts exports getJwtSecret() returning string.
    - NODE_ENV=production with missing JWT_SECRET throws Error containing JWT_SECRET.
    - NODE_ENV=production with JWT_SECRET=supersecret throws.
    - NODE_ENV=development without JWT_SECRET returns supersecret and logs warning once (module-level flag).
    - authRoutes.ts and adminRoutes.ts contain no literal supersecret string.
    - jwt.sign and jwt.verify use getJwtSecret().
  </acceptance_criteria>
  <action>
    Create server/src/lib/jwtSecret.ts per D-01/D-02/D-03. Remove duplicate JWT_SECRET constants from authRoutes.ts and adminRoutes.ts; import getJwtSecret instead.
  </action>
</task>

<task type="auto">
  <name>Task 2: Add login rate limiter on POST /login</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/authRoutes.ts
    - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/communityRoutes.ts
  </read_first>
  <files>server/src/routes/authRoutes.ts</files>
  <acceptance_criteria>
    - loginLimiter: windowMs 900000, max 5, standardHeaders true.
    - Applied only to router.post('/login', ...) not entire auth router.
    - 429 body is JSON like { error: 'Too many login attempts. Please try again later.' } with no username hints.
  </acceptance_criteria>
  <action>
    Copy rate-limit pattern from communityRoutes.ts. Create loginLimiter and attach to POST /login handler only per D-04/D-05.
  </action>
</task>

<task type="auto">
  <name>Task 3: Safe seed and gitignore for dev.db</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/src/seed.ts
    - /Users/anuragverma/Downloads/book website-frontend/.gitignore
    - /Users/anuragverma/Downloads/book website-frontend/.env.example
  </read_first>
  <files>server/src/seed.ts, .gitignore, .env.example, server/.env.example</files>
  <acceptance_criteria>
    - seed.ts admin upsert uses update: {} (no password in update branch).
    - .gitignore contains server/prisma/dev.db and *.db-journal.
    - .env.example lists JWT_SECRET with placeholder; removes VITE_MARKETING_MASTER_KEY line (defer MARKETING_* to plan 03 server .env.example).
  </acceptance_criteria>
  <action>
    Change prisma.admin.upsert update block to empty object. Append db paths to root .gitignore. If dev.db is git-tracked, run git rm --cached server/prisma/dev.db. Update env examples per D-24 (JWT only in this task).
  </action>
</task>

<task type="auto">
  <name>Task 4: Import jwtSecret at server boot</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/src/index.ts
    - /Users/anuragverma/Downloads/book website-frontend/server/src/lib/jwtSecret.ts
  </read_first>
  <files>server/src/index.ts</files>
  <acceptance_criteria>
    - index.ts imports getJwtSecret and calls it before app.listen so production misconfig exits early.
  </acceptance_criteria>
  <action>
    Add side-effect import: import { getJwtSecret } from './lib/jwtSecret'; getJwtSecret(); at top of index.ts after dotenv.config().
  </action>
</task>

</tasks>

<verification>
- [ ] `cd server && NODE_ENV=production JWT_SECRET= node -e "require('./dist/lib/jwtSecret')"` fails (after build) OR vitest SEC-01 test passes
- [ ] Supertest 6 rapid POST /api/v1/auth/login → last response 429
- [ ] Second seed run does not change admin password hash in dev.db
</verification>
