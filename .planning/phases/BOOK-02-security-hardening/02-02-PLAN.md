---
phase: 02-security-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - /Users/anuragverma/Downloads/book website-frontend/server/package.json
  - /Users/anuragverma/Downloads/book website-frontend/server/src/lib/sanitize.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/adminRoutes.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/communityRoutes.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/contentRoutes.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/schemas/content.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/index.ts
requirements:
  - SEC-03
  - SEC-07
autonomous: true
must_haves:
  truths:
    - "D-07: Blog article HTML sanitized server-side with DOMPurify allowlist before persist or public read."
    - "D-08: Community post/comment content rejects or strips HTML/script payloads on POST."
    - "D-09: Admin PATCH rejects customCss containing script, javascript:, expression( with 400 field error."
    - "D-11: Helmet CSP matches locked directive set including YouTube frame-src."
    - "D-12: Threat model documents unsafe-inline style-src tradeoff."
    - "D-10: Sanitization covers admin blog markdown and public community text only — no new moderation product."
  artifacts:
    - path: /Users/anuragverma/Downloads/book website-frontend/server/src/lib/sanitize.ts
      provides: sanitizeArticleHtml, stripCommunityText, validateCustomCss
  key_links:
    - from: /Users/anuragverma/Downloads/book website-frontend/server/src/routes/adminRoutes.ts
      to: /Users/anuragverma/Downloads/book website-frontend/server/src/lib/sanitize.ts
      via: article create/update + content PATCH
      pattern: sanitizeArticleHtml|validateCustomCss
---

<objective>
Mitigate XSS from markdown articles, community text, and admin customCss; configure explicit Helmet CSP.

Purpose: SEC-03, SEC-07.
Output: Server sanitization library, validators on mutating routes, production CSP headers.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/BOOK-02-security-hardening/02-CONTEXT.md
@.planning/phases/BOOK-02-security-hardening/02-RESEARCH.md
</execution_context>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-02-04 | Stored XSS in blog markdown | high | DOMPurify on article body (D-07) |
| T-02-05 | XSS via admin customCss | high | Reject dangerous patterns on PATCH (D-09) |
| T-02-06 | Community script injection | medium | Strip tags on POST (D-08) |
| T-02-07 | Missing CSP allows inline script | medium | Helmet contentSecurityPolicy (D-11) |
| T-02-08 | CSP breaks Vite dev | low | connect-src includes localhost ws in non-production |
</threat_model>

<context>
Blog renders via react-markdown in BlogPostPage.tsx — sanitize HTML/markdown output on server so client never receives raw script tags.
customCss still injected client-side via App.tsx styleTag — server rejection prevents new payloads; existing DB rows may need one-time admin fix manually.
CSP style-src 'unsafe-inline' accepted per D-12 until CSS moved server-side later.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sanitize library and install isomorphic-dompurify</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/package.json
    - /Users/anuragverma/Downloads/book website-frontend/.planning/phases/BOOK-02-security-hardening/02-CONTEXT.md
  </read_first>
  <files>server/package.json, server/src/lib/sanitize.ts</files>
  <acceptance_criteria>
    - isomorphic-dompurify in server/package.json dependencies.
    - sanitizeArticleHtml removes script tags from sample input.
    - ALLOWED_TAGS include h1-h6, p, a, img, ul, ol, li, code, pre, blockquote; FORBID script, iframe, on* attributes.
    - stripCommunityText removes angle-bracket tags from string.
    - validateCustomCss returns { ok: false, field: 'settings.customCss' } for strings containing &lt;script, javascript:, expression( case-insensitive.
  </acceptance_criteria>
  <action>
    npm install isomorphic-dompurify in server/. Create sanitize.ts exporting three functions per D-07/D-08/D-09.
  </action>
</task>

<task type="auto">
  <name>Task 2: Apply sanitization on admin article and content routes</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/adminRoutes.ts
    - /Users/anuragverma/Downloads/book website-frontend/server/src/schemas/content.ts
    - /Users/anuragverma/Downloads/book website-frontend/server/src/lib/sanitize.ts
  </read_first>
  <files>server/src/routes/adminRoutes.ts, server/src/schemas/content.ts</files>
  <acceptance_criteria>
    - POST/PATCH article routes run sanitizeArticleHtml on body field before prisma write (field name matches schema: body or content per prisma Article model).
    - PATCH /admin/content runs validateCustomCss when settings.customCss present; returns 400 { errors: [{ path: 'settings.customCss', message: '...' }] } on failure.
    - Public GET articles in contentRoutes returns sanitized body (defense in depth).
  </acceptance_criteria>
  <action>
    Wire sanitize into adminRoutes article handlers. Extend content PATCH handler to validate nested settings.customCss from req.body before merge. Sanitize on contentRoutes article serialization.
  </action>
</task>

<task type="auto">
  <name>Task 3: Sanitize community POST bodies</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/communityRoutes.ts
    - /Users/anuragverma/Downloads/book website-frontend/server/src/lib/sanitize.ts
  </read_first>
  <files>server/src/routes/communityRoutes.ts</files>
  <acceptance_criteria>
    - POST /posts and POST /posts/:id/comments run stripCommunityText on content before create.
    - Content containing literal &lt;script is stored without script tags (stripped or rejected with 400 — prefer strip per D-08).
  </acceptance_criteria>
  <action>
    Import stripCommunityText; apply to content field in create handlers before prisma.communityPost.create / comment.create.
  </action>
</task>

<task type="auto">
  <name>Task 4: Configure Helmet with explicit CSP</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/src/index.ts
    - /Users/anuragverma/Downloads/book website-frontend/.planning/phases/BOOK-02-security-hardening/02-CONTEXT.md
  </read_first>
  <files>server/src/index.ts</files>
  <acceptance_criteria>
    - helmet({ contentSecurityPolicy: { directives: { ... } } }) replaces bare helmet().
    - default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' https://fonts.gstatic.com; frame-src https://www.youtube.com https://www.youtube-nocookie.com; connect-src includes 'self' and in development http://localhost:5173 ws://localhost:5173.
    - GET /health response includes content-security-policy header.
  </acceptance_criteria>
  <action>
    Replace app.use(helmet()) with helmet.contentSecurityPolicy per D-11. Branch connect-src for NODE_ENV development vs production per D-12 comment in index.ts.
  </action>
</task>

</tasks>

<verification>
- [ ] Unit test: sanitizeArticleHtml('&lt;script&gt;alert(1)&lt;/script&gt;&lt;p&gt;ok&lt;/p&gt;') contains p, not script
- [ ] PATCH /admin/content with customCss `body { background: expression(alert(1)) }` → 400
- [ ] curl -I localhost:3001/health shows Content-Security-Policy header
</verification>
