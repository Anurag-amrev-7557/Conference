---
phase: 02-security-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/marketingRoutes.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/index.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/lib/corsPolicy.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/.env.example
  - /Users/anuragverma/Downloads/book website-frontend/.env.example
  - /Users/anuragverma/Downloads/book website-frontend/src/lib/marketing.ts
  - /Users/anuragverma/Downloads/book website-frontend/src/components/ContactSupportModal.tsx
requirements:
  - SEC-04
  - SEC-05
autonomous: true
must_haves:
  truths:
    - "D-16: POST /api/v1/marketing/webhook proxies to marketing-backend /webhook with server X-API-KEY."
    - "D-17: Client bundle has no VITE_MARKETING_MASTER_KEY or vellux_studio_2026_pk."
    - "D-18: Webhook body preserves WebhookPayload shape and content.source book_website."
    - "D-19: visitor_id forwarded from client body unchanged."
    - "D-20: email-agent/process not proxied; ContactSupportModal documents Phase 3 deferral."
    - "D-21: MARKETING_BACKEND_URL server-only in .env.example."
    - "D-13: ALLOWED_ORIGINS vs ADMIN_ALLOWED_ORIGINS split."
    - "D-14: Production denies requests without Origin except GET /health."
    - "D-15: credentials true only for admin origin matches."
  artifacts:
    - path: /Users/anuragverma/Downloads/book website-frontend/server/src/routes/marketingRoutes.ts
      provides: Server-side marketing webhook proxy
    - path: /Users/anuragverma/Downloads/book website-frontend/server/src/lib/corsPolicy.ts
      provides: Split public/admin CORS origin logic
  key_links:
    - from: /Users/anuragverma/Downloads/book website-frontend/src/lib/marketing.ts
      to: /Users/anuragverma/Downloads/book website-frontend/server/src/routes/marketingRoutes.ts
      via: POST /api/v1/marketing/webhook
      pattern: /marketing/webhook
---

<objective>
Proxy marketing telemetry through the book API, remove client-side API keys, and tighten CORS for production.

Purpose: SEC-04, SEC-05 — secure lead pipeline handoff to marketing-backend without browser secrets.
Output: marketingRoutes proxy, refactored MarketingService, split CORS policy.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/BOOK-02-security-hardening/02-CONTEXT.md
@.planning/phases/BOOK-02-security-hardening/02-RESEARCH.md
@/Users/anuragverma/Downloads/marketing-backend/marketing_api/main.py
</execution_context>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-02-09 | Stolen marketing master key from bundle | high | Server-only MARKETING_API_KEY (D-17) |
| T-02-10 | CSRF from arbitrary origin to admin API | high | ADMIN_ALLOWED_ORIGINS + no blind no-origin allow (D-14) |
| T-02-11 | Webhook spam via open proxy | medium | Optional rate limit on marketing router; validate JSON shape |
| T-02-12 | SSRF via MARKETING_BACKEND_URL | medium | Only forward to configured env URL; no client-supplied host |
</threat_model>

<context>
MarketingService.logEvent payload today matches WebhookPayload — keep id, type, actor, visitor_id, content fields.
API_BASE in src/lib/api.ts is http://localhost:3001/api/v1 — marketing fetch target `${API_BASE}/marketing/webhook`.
MARKETING_API_KEY reads process.env.MARKETING_API_KEY || process.env.VELLUX_API_KEY.
ContactSupportModal: add comment + user-visible message that email agent returns in Phase 3; do not add proxy for /email-agent/process (D-20).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketing webhook proxy route</name>
  <read_first>
    - /Users/anuragverma/Downloads/marketing-backend/marketing_api/main.py
    - /Users/anuragverma/Downloads/book website-frontend/src/lib/marketing.ts
    - /Users/anuragverma/Downloads/book website-frontend/src/lib/api.ts
  </read_first>
  <files>server/src/routes/marketingRoutes.ts, server/src/index.ts, server/.env.example</files>
  <acceptance_criteria>
    - marketingRoutes.ts exports POST /webhook handler.
    - Reads MARKETING_BACKEND_URL (default http://localhost:8000) and MARKETING_API_KEY from env.
    - Forwards req.body to ${MARKETING_BACKEND_URL}/webhook with header X-API-KEY and Content-Type application/json.
    - Returns upstream status and JSON body; 502 on network failure.
    - index.ts mounts app.use('/api/v1/marketing', marketingRoutes).
    - server/.env.example documents MARKETING_API_KEY, MARKETING_BACKEND_URL, VELLUX_API_KEY alias note.
  </acceptance_criteria>
  <action>
    Create marketingRoutes.ts with express.Router POST /webhook using fetch (Node 18+). Validate body has id, type, actor.email strings before forward. Mount in index.ts.
  </action>
</task>

<task type="auto">
  <name>Task 2: Refactor MarketingService to use book API proxy</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/src/lib/marketing.ts
    - /Users/anuragverma/Downloads/book website-frontend/src/lib/api.ts
    - /Users/anuragverma/Downloads/book website-frontend/.env.example
  </read_first>
  <files>src/lib/marketing.ts, .env.example</files>
  <acceptance_criteria>
    - marketing.ts contains no MASTER_API_KEY, VITE_MARKETING_MASTER_KEY, or vellux_studio_2026_pk strings.
    - fetch URL is ${API_BASE}/marketing/webhook where API_BASE matches api.ts pattern.
    - Request headers are Content-Type application/json only (no X-API-KEY).
    - Body still includes id, type, actor, visitor_id, content with source book_website.
    - Root .env.example removes VITE_MARKETING_HUB_URL and VITE_MARKETING_MASTER_KEY lines.
  </acceptance_criteria>
  <action>
    Remove client key and external marketing URL. Import or duplicate API_BASE constant from api.ts (prefer shared export from api.ts: export const API_BASE). Point logEvent fetch to book proxy per D-17/D-18/D-19.
  </action>
</task>

<task type="auto">
  <name>Task 3: Split CORS policy for public vs admin</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/src/index.ts
    - /Users/anuragverma/Downloads/book website-frontend/.planning/phases/BOOK-02-security-hardening/02-CONTEXT.md
  </read_first>
  <files>server/src/lib/corsPolicy.ts, server/src/index.ts, server/.env.example</files>
  <acceptance_criteria>
    - corsPolicy.ts exports createCorsMiddleware() using ALLOWED_ORIGINS and ADMIN_ALLOWED_ORIGINS env lists.
    - Paths starting with /api/v1/admin or mutating /api/v1/auth use admin list; /api/v1/content and /api/v1/community use public list.
    - NODE_ENV=production: missing Origin callback returns false except req.path === '/health' on GET.
    - credentials: true only when origin matches admin list for admin routes.
    - server/.env.example documents ALLOWED_ORIGINS and ADMIN_ALLOWED_ORIGINS.
  </acceptance_criteria>
  <action>
    Extract CORS from index.ts into corsPolicy.ts per D-13/D-14/D-15. Replace single cors() block with dynamic origin function inspecting req.path and method.
  </action>
</task>

<task type="auto">
  <name>Task 4: Defer ContactSupportModal email-agent to Phase 3</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/src/components/ContactSupportModal.tsx
    - /Users/anuragverma/Downloads/book website-frontend/.planning/phases/BOOK-02-security-hardening/02-CONTEXT.md
  </read_first>
  <files>src/components/ContactSupportModal.tsx</files>
  <acceptance_criteria>
    - Component file comment states email-agent proxy is Phase 3 (MKT-*).
    - handleSubmit sets error status with message mentioning support contact temporarily unavailable OR uses mailto fallback — does not call marketing-backend :8000 directly.
    - No import.meta.env.VITE_MARKETING_HUB_URL in this file.
  </acceptance_criteria>
  <action>
    Remove direct fetch to email-agent/process per D-20. Show honest UX that full agent integration ships Phase 3; optional mailto: link for support@bookwebsite.com.
  </action>
</task>

<task type="auto">
  <name>Task 5: Verify no marketing secrets in production build</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/package.json
    - /Users/anuragverma/Downloads/book website-frontend/src/lib/marketing.ts
  </read_first>
  <files>package.json</files>
  <acceptance_criteria>
    - npm run build exits 0.
    - rg -l vellux_studio_2026_pk dist/ returns no matches.
    - rg -l VITE_MARKETING_MASTER_KEY dist/ returns no matches.
  </acceptance_criteria>
  <action>
    Run production build from repo root. Grep dist/ for leaked key strings. Fix any remaining references if found.
  </action>
</task>

</tasks>

<verification>
- [ ] curl -X POST http://localhost:3001/api/v1/marketing/webhook -H 'Content-Type: application/json' -d '{"id":"t","type":"page_view","actor":{"email":"a@b.com"},"visitor_id":"vid_1","content":{"source":"book_website"}}' returns 200 when marketing-backend running
- [ ] Built dist/ contains no vellux_studio_2026_pk
- [ ] Production CORS: curl admin route without Origin header → blocked
</verification>
