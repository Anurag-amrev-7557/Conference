---
phase: 34-conference-admin-crm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/websiteData.ts
  - src/lib/conferenceDefaults.ts
  - src/components/WebsiteDataProvider.tsx
  - server/src/seed.ts
  - src/components/sections/conference/*.tsx
  - src/pages/ConferencePage.tsx
  - src/seo/seoConfig.ts
  - src/components/admin/SettingsManager.tsx
requirements:
  - CONF-ADM-01
  - CONF-ADM-02
  - CONF-ADM-05
---

<objective>
Add `settings.conference`, seed defaults, merge in provider, wire public conference sections + `/conference` route SEO.
</objective>

<tasks>

<task type="auto">
  <name>Types, defaults, seed, merge</name>
  <action>
Add `ConferenceContent` under `SiteSettings.conference`. Export `defaultConferenceContent` from current hardcoded values. Deep-merge in `WebsiteDataProvider` when loading settings. Add conference blob to `server/src/seed.ts` settings JSON.
  </action>
</task>

<task type="auto">
  <name>Wire public page</name>
  <action>
Conference sections read `settings.conference` with defaults. `ConferencePage` uses `usePageSeo` (remove static Helmet). Add `/conference` to `routeSeo` type + `seoConfig` + Settings route SEO fields.
  </action>
</task>

</tasks>
