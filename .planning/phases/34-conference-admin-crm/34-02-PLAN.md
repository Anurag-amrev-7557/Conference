---
phase: 34-conference-admin-crm
plan: 02
type: execute
wave: 2
depends_on:
  - 34-01-PLAN.md
files_modified:
  - src/components/admin/ConferenceManager.tsx
  - src/pages/AdminPage.tsx
  - src/components/admin/AdminLayout.tsx
requirements:
  - CONF-ADM-03
---

<objective>
One slim admin screen: Hero tab, Sections tab (eyebrows/titles), Lists tab (speakers, agenda days, FAQ, logos). Single Save → `updateSettings`.
</objective>

<tasks>

<task type="auto">
  <name>ConferenceManager (3 tabs)</name>
  <action>
Build `ConferenceManager` like Settings catalog tab: labeled inputs, add/remove rows for lists, one Save button with loading/error state. `setPreview` updates `settings.conference` only.
  </action>
</task>

<task type="auto">
  <name>Admin route</name>
  <action>
Add `/admin/conference` + sidebar link. No custom preview iframe — include link “Open /conference” in header.
  </action>
</task>

</tasks>
