---
phase: 24-marketing-stack-production-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
files_modified:
  - marketing-backend/alembic.ini
  - marketing-backend/alembic/
  - marketing-backend/tests/test_smoke.py
  - marketing-backend/MIGRATION.md
autonomous: false
requirements:
  - MKT-03
  - MKT-04
must_haves:
  truths:
    - Alembic scaffold exists (no migration applied)
    - Smoke test suite validates critical flows locally
  artifacts:
    - marketing-backend/tests/test_smoke.py
    - marketing-backend/MIGRATION.md
  key_links: []
---

<objective>
Prepare the backend for an eventual Postgres migration by adding an Alembic scaffold and writing pytest smoke tests that exercise webhook, lead profile, and email-agent flows (happy path + key error cases). Produce `MIGRATION.md` documenting the Phase 25 cutover steps.
</objective>

<execution_context>
@.planning/phases/24-marketing-stack-production-hardening/24-RESEARCH.md
@marketing-backend/
</execution_context>

<context>
@marketing-backend/marketing_api/
</context>

<tasks>

<task type="manual">
  <name>Task 1: Scaffold Alembic (no migrations applied)</name>
  <files>marketing-backend/alembic.ini, marketing-backend/alembic/</files>
  <action>
Initialize Alembic in the backend directory (`alembic init alembic`). Commit the scaffold and document that no production migration should be applied until Phase 25. Ensure `env.py` reads `DATABASE_URL` env var.
  </action>
  <verify>
test -d marketing-backend/alembic || true
  </verify>
  <acceptance_criteria>
    - `alembic/` directory exists and `alembic.ini` references `DATABASE_URL`
  </acceptance_criteria>
  <done>Alembic scaffold present
</task>

<task type="manual">
  <name>Task 2: Add pytest smoke tests</name>
  <files>marketing-backend/tests/test_smoke.py</files>
  <action>
Create `tests/test_smoke.py` using FastAPI TestClient or HTTPX to exercise:
  - POST `/webhook` happy path stores a lead (or returns 201)
  - POST `/leads/{id}/profile` triggers expected response
  - POST `/email-agent/process` returns 200 or documented fallback
Also include negative tests for missing API key (403) and malformed JSON (422).
  </action>
  <verify>
pytest -q marketing-backend/tests/test_smoke.py -q || true
  </verify>
  <acceptance_criteria>
    - `tests/test_smoke.py` exists and is runnable locally
    - Tests assert expected status codes for happy and error cases
  </acceptance_criteria>
  <done>Smoke tests exist and runnable locally
</task>

<task type="manual">
  <name>Task 3: Write `MIGRATION.md` documenting Phase 25 cutover</name>
  <files>marketing-backend/MIGRATION.md</files>
  <action>
Document the high-level migration plan: create Postgres instance, set `DATABASE_URL`, run alembic upgrades in staging, run data export/import steps, test smoke suite, and rollback steps. Keep exact commands as examples but note that real infra will depend on chosen provider (RDS, self-hosted).
  </action>
  <verify>
test -f marketing-backend/MIGRATION.md || true
  </verify>
  <acceptance_criteria>
    - MIGRATION.md covers export/import, migration dry-run, verification steps, and rollback
  </acceptance_criteria>
  <done>Migration playbook documented for Phase 25
</task>

</tasks>

<verification>
- [ ] Alembic scaffold present
- [ ] `pytest tests/test_smoke.py` runs locally (or is runnable with minimal env)
- [ ] MIGRATION.md created and linked from phase context
</verification>

<success_criteria>
- Backend is prepared for Postgres migration and has smoke tests that will run after deploys to validate production behavior.
</success_criteria>
