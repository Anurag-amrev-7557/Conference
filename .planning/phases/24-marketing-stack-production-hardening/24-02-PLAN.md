---
phase: 24-marketing-stack-production-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - marketing-backend/marketing_api/main.py
  - marketing-backend/marketing_api/health.py
  - marketing-backend/marketing_api/__init__.py
  - marketing-backend/requirements.txt
autonomous: false
requirements:
  - MKT-01
  - MKT-04
must_haves:
  truths:
    - `/health/live` and `/health/ready` endpoints exist
    - readiness checks include DB connectivity
  artifacts:
    - marketing-backend/marketing_api/health.py
  key_links: []
---

<objective>
Add liveness and readiness probes to the marketing backend, implement runtime env validation for critical vars, and provide simple startup logging to aid operators.
</objective>

<execution_context>
@.planning/phases/24-marketing-stack-production-hardening/24-CONTEXT.md
@.planning/phases/24-marketing-stack-production-hardening/24-RESEARCH.md
</execution_context>

<context>
@marketing-backend/marketing_api/
</context>

<tasks>

<task type="manual">
  <name>Task 1: Implement `/health/live` and `/health/ready`</name>
  <files>marketing-backend/marketing_api/health.py, marketing-backend/marketing_api/main.py</files>
  <read_first>marketing-backend/marketing_api/main.py</read_first>
  <action>
Create a `health.py` module that exposes two FastAPI endpoints: `/health/live` (process alive) and `/health/ready` (database connectivity + optional external checks). Wire routes into `main.py` and ensure JSON responses include timestamps and per-check statuses.
  </action>
  <verify>
python -c "import requests; print('skipping live check in unit plan')" || true
  </verify>
  <acceptance_criteria>
    - `health.py` exists and is imported by `main.py`
    - `/health/live` returns 200 and `/health/ready` returns 200 when DB is reachable
  </acceptance_criteria>
  <done>Health endpoints implemented</done>
</task>

<task type="manual">
  <name>Task 2: Add env validation and startup logging</name>
  <files>marketing-backend/marketing_api/main.py</files>
  <action>
On startup, validate presence of critical env vars (e.g., `ALLOWED_ORIGINS`, `VELLUX_API_KEY` if agent enabled). Log warnings for optional vars. Fail-fast only for truly required vars. Add structured startup logs (JSON lines) describing configured origins and runtime mode.
  </action>
  <verify>
grep -q "ALLOWED_ORIGINS" marketing-backend/marketing_api/main.py
  </verify>
  <acceptance_criteria>
    - Startup procedure performs env checks and logs configured origins
    - Missing required vars cause a clear error message
  </acceptance_criteria>
  <done>Runtime validation and startup logs added</done>
</task>

<task type="manual">
  <name>Task 3: Add basic dependency entries for pytest and httpx in requirements.txt</name>
  <files>marketing-backend/requirements.txt</files>
  <action>
Append `pytest` and `httpx` (or `requests`) to `requirements.txt` to prepare for smoke tests run during Phase 24 verification and Phase 25 execution.
  </action>
  <verify>
grep -q "pytest" marketing-backend/requirements.txt || true
  </verify>
  <acceptance_criteria>
    - requirements.txt lists `pytest` and `httpx` or equivalent test libs
  </acceptance_criteria>
  <done>Test dependencies added to requirements
</task>

</tasks>

<verification>
- [ ] `/health/live` and `/health/ready` present and documented
- [ ] Startup logs show configured origins and mode
- [ ] requirements.txt updated with test deps
</verification>

<success_criteria>
- Backend exposes Kubernetes-friendly probes and startup validation; operators can determine readiness and troubleshoot quickly.
</success_criteria>
