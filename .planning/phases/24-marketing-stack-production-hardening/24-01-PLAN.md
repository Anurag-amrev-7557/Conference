---
phase: 24-marketing-stack-production-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - marketing-backend/ENV-SCHEMA.md
  - marketing-frontend/ENV-SCHEMA.md
  - marketing-frontend/.env.example
  - marketing-backend/marketing_api/main.py
  - marketing-frontend/src/lib/api.ts
autonomous: false
requirements:
  - MKT-01
  - MKT-02
must_haves:
  truths:
    - CORS middleware enabled and driven by `ALLOWED_ORIGINS` env var
    - Frontend uses `VITE_MARKETING_API_URL` and has `.env.example`
  artifacts:
    - marketing-backend/ENV-SCHEMA.md
    - marketing-frontend/ENV-SCHEMA.md
    - marketing-frontend/.env.example
  key_links: []
---

<objective>
Unify service contract: enable and configure CORS in the backend, publish environment variable schemas for both services, and align the frontend to use `VITE_MARKETING_API_URL`.
</objective>

<execution_context>
@.planning/phases/24-marketing-stack-production-hardening/24-CONTEXT.md
@.planning/phases/24-marketing-stack-production-hardening/24-RESEARCH.md
</execution_context>

<context>
@marketing-backend/marketing_api/
@marketing-frontend/src/
</context>

<tasks>

<task type="manual">
  <name>Task 1: Enable CORS middleware in backend</name>
  <files>marketing-backend/marketing_api/main.py</files>
  <read_first>marketing-backend/marketing_api/main.py</read_first>
  <action>
Uncomment or add `CORSMiddleware` setup and wire it to read from `ALLOWED_ORIGINS` env var (comma separated). Ensure `allow_credentials` is false and allowed methods/headers are explicit. Add a small startup log that prints the configured origins.
  </action>
  <verify>
grep -q "ALLOWED_ORIGINS" marketing-backend/marketing_api/main.py && grep -q "CORSMiddleware" marketing-backend/marketing_api/main.py
  </verify>
  <acceptance_criteria>
    - `main.py` contains CORSMiddleware configured from `ALLOWED_ORIGINS`
    - No wildcard `*` origins used in production sample
  </acceptance_criteria>
  <done>CORS enabled and configured from env</done>
</task>

<task type="manual">
  <name>Task 2: Create ENV-SCHEMA.md for backend and frontend</name>
  <files>marketing-backend/ENV-SCHEMA.md, marketing-frontend/ENV-SCHEMA.md</files>
  <action>
Create `ENV-SCHEMA.md` documenting required and optional env vars, examples, and production notes for both `marketing-backend` and `marketing-frontend`. Add `.env.example` to frontend with `VITE_MARKETING_API_URL` placeholder.
  </action>
  <verify>
test -f marketing-backend/ENV-SCHEMA.md && test -f marketing-frontend/ENV-SCHEMA.md && test -f marketing-frontend/.env.example
  </verify>
  <acceptance_criteria>
    - ENV-SCHEMA.md lists `ALLOWED_ORIGINS`, `DATABASE_URL`, `VELLUX_API_KEY`, `ENVIRONMENT`, and CLOUD creds where relevant
    - frontend `.env.example` contains a `VITE_MARKETING_API_URL` entry
  </acceptance_criteria>
  <done>Env schemas and examples created</done>
</task>

<task type="manual">
  <name>Task 3: Align frontend API client to VITE_MARKETING_API_URL</name>
  <files>marketing-frontend/src/lib/api.ts</files>
  <read_first>marketing-frontend/src/lib/api.ts</read_first>
  <action>
Update the frontend API client to use `import.meta.env.VITE_MARKETING_API_URL` as the base URL. Add a fallback to `''` for local dev but document that production builds must set the env.
  </action>
  <verify>
grep -q "VITE_MARKETING_API_URL" marketing-frontend/src/lib/api.ts
  </verify>
  <acceptance_criteria>
    - `api.ts` references `VITE_MARKETING_API_URL`
    - Frontend `.env.example` documents how to set it for production
  </acceptance_criteria>
  <done>Frontend base URL aligned to env var</done>
</task>

</tasks>

<verification>
- [ ] ENV-SCHEMA.md and .env.example present
- [ ] CORS configured in backend main.py
- [ ] Frontend references `VITE_MARKETING_API_URL`
</verification>

<success_criteria>
- Service contract artifacts exist and are documented. Frontend and backend agree on origin and API env names.
</success_criteria>
