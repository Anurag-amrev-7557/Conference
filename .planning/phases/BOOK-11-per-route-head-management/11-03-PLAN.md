---
phase: 11-per-route-head-management
plan: 03
type: tdd
wave: 3
depends_on:
  - 11-01
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - src/seo/seoConfig.test.ts
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
  - META-05
autonomous: true
must_haves:
  truths:
    - "resolvePageSeo unit tests cover admin, community, article, missing slug, and static route branches."
    - "Canonical and ogImage assertions use absolute URLs when site origin is set."
    - "CI-friendly single-run vitest command exists for seoConfig."
  artifacts:
    - path: src/seo/seoConfig.test.ts
      provides: Automated META-01–05 resolver coverage
    - path: vitest.config.ts
      provides: Vitest + jsdom/happy-dom for pure TS tests
  key_links:
    - from: src/seo/seoConfig.test.ts
      to: src/seo/seoConfig.ts
      via: direct import resolvePageSeo
      pattern: resolvePageSeo
---

<objective>
Add Vitest and unit tests for resolvePageSeo so META-01–05 resolver behavior is regression-safe before Phase 14 prerender reuse.

Purpose: Nyquist validation for resolver branches — faster feedback than manual DevTools-only checks.
Output: vitest.config.ts, seoConfig.test.ts, npm test script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/BOOK-11-per-route-head-management/11-RESEARCH.md (Testing Approach)
@.planning/phases/BOOK-11-per-route-head-management/11-VALIDATION.md
</execution_context>

<context>
@src/seo/seoConfig.ts
@src/seo/routes.ts
@src/lib/websiteData.ts

Tests must call setSiteOrigin('https://example.com') in beforeEach and reset in afterEach. Use minimal WebsiteData fixture objects — no React, no router.
</context>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-11-SC | Vitest devDependency supply chain | low | vitest from npm; pin version in package-lock on install |
| T-11-06 | False confidence from untested branches | medium | Test matrix maps 1:1 to META-01–05 resolver cases in 11-VALIDATION.md |
</threat_model>

<feature>
  <name>resolvePageSeo unit tests</name>
  <files>src/seo/seoConfig.test.ts, vitest.config.ts</files>
  <behavior>
    - META-01: / vs /blog produce different title strings from routeDefaults.
    - META-02: /blog/my-post canonical === https://example.com/blog/my-post (no trailing slash).
    - META-03: article route ogType === 'article'; / ogType === 'website'.
    - META-04: resolved output includes ogImage absolute URL; twitter card constant in SeoHead tested separately or document ogImage absolute in PageSeo.
    - META-05: /admin/settings and /dashboard robots === 'noindex,nofollow'.
    - D-09: /blog/ghost without article → noindex + canonical https://example.com/blog.
    - D-14: /community → noindex.
    - D-15: unpublished or noindex article → robots noindex.
    - D-19: googleSiteVerification on / when set; absent on /admin.
  </behavior>
  <implementation>
    Add vitest and @vitest/coverage-v8 or minimal vitest only as devDependencies. Create vitest.config.ts with environment node (pure functions). Add package.json script "test": "vitest run". Implement seoConfig.test.ts with fixtures for settings.seo and articles. RED: run vitest before implementation fixes if any test fails on existing code. GREEN: all cases pass.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Add Vitest toolchain</name>
  <files>package.json, package-lock.json, vitest.config.ts</files>
  <acceptance_criteria>
    - devDependencies include vitest (latest stable compatible with Vite 8).
    - package.json scripts include "test": "vitest run".
    - vitest.config.ts includes src/**/*.test.ts and resolves @/ or relative imports to src/.
  </acceptance_criteria>
  <action>
    Install vitest as devDependency. Add vitest.config.ts at repo root (match Vite 8 project). Do not add watch-mode scripts. No E2E framework.
  </action>
  <verify>
    <automated>npx vitest run --passWithNoTests 2>&1 | tail -3</automated>
  </verify>
  <done>Vitest runs (empty or passing) from repo root.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement seoConfig.test.ts coverage matrix</name>
  <files>src/seo/seoConfig.test.ts</files>
  <behavior>
    - See feature behavior block — minimum 10 distinct cases covering all META-01–05 resolver branches.
    - setSiteOrigin('https://example.com') before each test; clear after.
  </behavior>
  <acceptance_criteria>
    - src/seo/seoConfig.test.ts exists with cases from 11-RESEARCH Testing Approach table.
    - npx vitest run src/seo/seoConfig.test.ts exits 0.
    - Tests import only seoConfig, siteUrl, websiteData types — no @testing-library/react required for resolver-only tests.
  </acceptance_criteria>
  <action>
  Write failing tests first (RED), then fix resolvePageSeo if any edge case fails (GREEN). Cover: static /, /blog listing, published article, noindex article, unpublished article, missing slug, /events, /community noindex, /admin/*, /dashboard, not-found path. Assert canonical host is example.com. Assert ogImage starts with https:// when fallback /og-image.jpg used.
  </action>
  <verify>
    <automated>npx vitest run src/seo/seoConfig.test.ts</automated>
  </verify>
  <done>All resolver tests green; META-01–05 logic locked by automation.</done>
</task>

</tasks>

<verification>
- [ ] `npm test` or `npx vitest run src/seo/seoConfig.test.ts` exits 0
- [ ] 11-VALIDATION.md task rows for 11-03-02 marked with file exists
</verification>

<success_criteria>
- Vitest installed and seoConfig.test.ts passes
- Nyquist wave_0_complete can flip true in 11-VALIDATION.md after this plan
</success_criteria>

<output>
Create `.planning/phases/BOOK-11-per-route-head-management/11-03-SUMMARY.md` when done
</output>
