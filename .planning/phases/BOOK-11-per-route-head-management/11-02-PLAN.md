---
phase: 11-per-route-head-management
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/pages/LandingPage.tsx
  - src/pages/BlogPage.tsx
  - src/pages/BlogPostPage.tsx
  - src/pages/EventsPage.tsx
  - src/pages/CommunityPage.tsx
  - src/pages/AdminPage.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/NotFoundPage.tsx
  - src/components/admin/BlogManager.tsx
  - src/components/admin/SettingsManager.tsx
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
  - META-05
autonomous: true
must_haves:
  truths:
    - "META-01: /, /blog, /blog/:slug, /events show unique title and description after navigation."
    - "META-02: Each wired route has one canonical href = siteUrl + normalized path."
    - "META-03/META-04: Indexable routes emit full OG and Twitter sets with summary_large_image."
    - "META-05: /admin, /admin/*, /dashboard emit noindex,nofollow."
    - "D-09: Missing blog slug renders SeoHead with noindex before redirect."
    - "D-14/D-16: /community and * NotFound use noindex,nofollow."
  artifacts:
    - path: src/pages/BlogPostPage.tsx
      provides: SeoHead before early return on missing article
    - path: src/pages/LandingPage.tsx
      provides: usePageSeo + SeoHead at fragment root
  key_links:
    - from: src/pages/BlogPostPage.tsx
      to: src/seo/usePageSeo.ts
      via: article passed from slug lookup
      pattern: usePageSeo.*article
    - from: src/pages/LandingPage.tsx
      to: src/seo/SeoHead.tsx
      via: first child of page JSX
      pattern: SeoHead
---

<objective>
Wire SeoHead on every route and update admin SEO help copy so live head behavior matches editor expectations.

Purpose: Deliver META-01–05 — unique per-route head tags, robots policy, and present-tense admin guidance.
Output: All marketing/admin/404 pages emit resolved head; admin microcopy per 11-UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/BOOK-11-per-route-head-management/11-01-SUMMARY.md
@.planning/phases/BOOK-11-per-route-head-management/11-UI-SPEC.md
@.planning/phases/BOOK-11-per-route-head-management/11-CONTEXT.md
</execution_context>

<context>
@src/seo/usePageSeo.ts
@src/seo/SeoHead.tsx
@src/pages/BlogPostPage.tsx
@.planning/phases/BOOK-11-per-route-head-management/11-UI-SPEC.md (Copywriting Contract, Browser Verification Checklist)

Pattern: hooks first, then SeoHead as first JSX child, then layout (PATTERNS.md page composition order).
</context>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-11-03 | Soft 404 — missing slug indexed | high | BlogPostPage renders SeoHead before `if (!article) return null` (D-09, Pitfall 4) |
| T-11-04 | Duplicate canonical from shell + Helmet | medium | Verify single canonical after navigation (META-02 manual) |
| T-11-05 | Unpublished article indexed | medium | Resolver noindex when !isPublished (D-15) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire SeoHead on public marketing pages</name>
  <read_first>
    - src/pages/LandingPage.tsx
    - src/pages/BlogPage.tsx
    - src/pages/BlogPostPage.tsx
    - src/pages/EventsPage.tsx
    - src/pages/CommunityPage.tsx
    - .planning/phases/BOOK-11-per-route-head-management/11-RESEARCH.md (BlogPostPage integration)
  </read_first>
  <files>src/pages/LandingPage.tsx, src/pages/BlogPage.tsx, src/pages/BlogPostPage.tsx, src/pages/EventsPage.tsx, src/pages/CommunityPage.tsx</files>
  <acceptance_criteria>
    - LandingPage, BlogPage, EventsPage: import SeoHead and usePageSeo; render SeoHead as first element in return tree.
    - BlogPostPage: compute article via data.articles.find(slug); call usePageSeo({ article }) before redirect effect; render SeoHead even when !article (D-09).
    - BlogPostPage: keep existing navigate('/blog') on missing slug; move `if (!article) return null` to after SeoHead fragment wrapper.
    - CommunityPage: SeoHead present with noindex from resolver (D-14).
    - No page imports Helmet directly — only SeoHead (D-02).
    - After API hydration with siteUrl, canonical host matches getSiteOrigin() not hardcoded monograph (D-04).
  </acceptance_criteria>
  <action>
    Wire per D-02. BlogPostPage critical fix per 11-RESEARCH: return fragment with SeoHead first, then conditional article UI. Public pages use usePageSeo() without article arg except BlogPostPage passes resolved article (may be undefined). Do not add JSON-LD or new visual UI.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>META-01–04 satisfied on public routes after client navigation (DevTools head check).</done>
</task>

<task type="auto">
  <name>Task 2: Wire noindex routes — admin, dashboard, not found</name>
  <read_first>
    - src/pages/AdminPage.tsx
    - src/pages/DashboardPage.tsx
    - src/pages/NotFoundPage.tsx
    - src/seo/seoConfig.ts
  </read_first>
  <files>src/pages/AdminPage.tsx, src/pages/DashboardPage.tsx, src/pages/NotFoundPage.tsx</files>
  <acceptance_criteria>
    - AdminPage: SeoHead at top; usePageSeo or resolvePageSeo with current pathname (useLocation); robots noindex,nofollow on /admin and nested paths (D-13, META-05).
    - DashboardPage: SeoHead with noindex,nofollow (D-13).
    - NotFoundPage: useLocation + SeoHead; robots noindex,nofollow (D-16).
    - Admin routes omit google-site-verification meta (D-19).
  </acceptance_criteria>
  <action>
    Add SeoHead to admin/dashboard/404 pages. Dashboard may use usePageSeo() without extra data — resolver admin branch handles robots. NotFound uses pathname from useLocation (typically unmatched path). No sitemap changes (Phase 13).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>META-05: admin and dashboard routes emit noindex,nofollow via Helmet.</done>
</task>

<task type="auto">
  <name>Task 3: Admin SEO help microcopy (11-UI-SPEC)</name>
  <read_first>
    - .planning/phases/BOOK-11-per-route-head-management/11-UI-SPEC.md (Copywriting Contract)
    - src/components/admin/BlogManager.tsx
    - src/components/admin/SettingsManager.tsx
  </read_first>
  <files>src/components/admin/BlogManager.tsx, src/components/admin/SettingsManager.tsx</files>
  <acceptance_criteria>
    - BlogManager Search & Social: help line under SEO title/description per UI-SPEC ("Leave blank to use the article title and short summary on the live site.").
    - BlogManager noindex toggle subtitles match UI-SPEC on/off states (D-15 editor-facing copy).
    - SettingsManager SEO tab: help under Default OG and GSC fields per UI-SPEC exact strings.
    - Help styling: text-[11px] text-muted leading-relaxed with aria-describedby on controls.
    - No new admin fields, tabs, or SERP/social preview components (deferred Phase 15).
    - grep src for "after Phase 11" returns 0.
  </acceptance_criteria>
  <action>
    Update microcopy only per 11-UI-SPEC Copywriting Contract. Remove any "after Phase 11" deferred wording. Do not change save payloads or layout structure beyond hint lines.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && ! grep -r 'after Phase 11' src/components/admin 2>/dev/null</automated>
  </verify>
  <done>Admin help reflects live head behavior; no Phase 15 preview added.</done>
</task>

</tasks>

<verification>
- [ ] Navigate / → /blog → /blog/{slug}: tab title changes; `document.querySelectorAll('link[rel=canonical]').length === 1`
- [ ] /admin: meta robots contains noindex
- [ ] /community: robots noindex; tags still present
- [ ] Invalid /blog/bad-slug: noindex visible before redirect
- [ ] 11-UI-SPEC Browser Verification Checklist spot-check (META-01–06)
</verification>

<success_criteria>
- All routes in App.tsx render SeoHead
- META-01–05 observable in DevTools after SPA navigation
- Admin copy matches 11-UI-SPEC
</success_criteria>

<output>
Create `.planning/phases/BOOK-11-per-route-head-management/11-02-SUMMARY.md` when done
</output>
