---
phase: 11-per-route-head-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/main.tsx
  - src/App.tsx
  - index.html
  - public/og-image.jpg
  - src/seo/types.ts
  - src/seo/routes.ts
  - src/seo/seoConfig.ts
  - src/seo/SeoHead.tsx
  - src/seo/usePageSeo.ts
requirements:
  - META-06
autonomous: true
must_haves:
  truths:
    - "D-01: HelmetProvider wraps app in main.tsx outside Router."
    - "D-02: src/seo/ module exists with resolvePageSeo, SeoHead, usePageSeo, routes registry."
    - "D-03: ThemeSynchronizer no longer sets document.title or meta description."
    - "D-17/D-18: index.html has no duplicate canonical, OG, or Twitter blocks."
    - "D-06 fallback: public/og-image.jpg exists for absoluteUrl('/og-image.jpg')."
  artifacts:
    - path: src/seo/seoConfig.ts
      provides: Pure resolvePageSeo (no React imports)
      exports: ["resolvePageSeo", "normalizeCanonicalPath", "resolveImageUrl"]
    - path: src/seo/SeoHead.tsx
      provides: Single Helmet tag emitter
    - path: src/main.tsx
      provides: HelmetProvider + WebsiteDataProvider shell
  key_links:
    - from: src/seo/seoConfig.ts
      to: src/seo/siteUrl.ts
      via: absoluteUrl for canonical and images
      pattern: "import.*absoluteUrl.*from.*siteUrl"
    - from: src/main.tsx
      to: src/seo/SeoHead.tsx
      via: HelmetProvider enables Helmet reconciliation
      pattern: HelmetProvider
---

<objective>
Install react-helmet-async, build the centralized src/seo/ head pipeline, mount providers, and trim the static HTML shell.

Purpose: META-06 and foundation for META-01–05 — eliminate duplicate shell tags and global ThemeSynchronizer SEO before per-page wiring.
Output: seo module, HelmetProvider shell, trimmed index.html, default OG asset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/BOOK-11-per-route-head-management/11-CONTEXT.md
@.planning/phases/BOOK-11-per-route-head-management/11-RESEARCH.md
@.planning/phases/BOOK-11-per-route-head-management/PATTERNS.md
@.planning/phases/BOOK-11-per-route-head-management/11-UI-SPEC.md
</execution_context>

<context>
@src/seo/siteUrl.ts
@src/components/WebsiteDataProvider.tsx
@src/App.tsx
@index.html
@package.json

<interfaces>
From src/seo/siteUrl.ts (Phase 10 — consume, do not duplicate):
- setSiteOrigin(url: string): void
- getSiteOrigin(): string
- absoluteUrl(path: string): string — returns relative path if origin unset

From src/lib/websiteData.ts:
- Article: seoTitle?, seoDescription?, ogImage?, noindex?, isPublished, slug, title, excerpt
- SiteSettings.seo: title, description, ogImage?, googleSiteVerification?
- WebsiteData: settings, articles, events, ...

WebsiteDataProvider already calls setSiteOrigin when API siteUrl is present.
</interfaces>
</context>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-11-01 | XSS via CMS SEO strings in meta tags | medium | SeoHead uses JSX text props only — no dangerouslySetInnerHTML |
| T-11-02 | Canonical spoofing via query/hash | low | normalizeCanonicalPath uses router pathname only |
| T-11-SC | Supply-chain risk on npm install | medium | react-helmet-async@3.0.0 locked per D-01; RESEARCH Package Legitimacy Audit = Approved (github.com/staylor/react-helmet-async) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Install react-helmet-async and mount provider shell</name>
  <read_first>
    - .planning/phases/BOOK-11-per-route-head-management/11-CONTEXT.md (D-01)
    - .planning/phases/BOOK-11-per-route-head-management/11-RESEARCH.md (Pattern 2)
    - src/main.tsx
    - src/App.tsx
  </read_first>
  <files>package.json, package-lock.json, src/main.tsx, src/App.tsx</files>
  <acceptance_criteria>
    - package.json dependencies include "react-helmet-async": "3.0.0" (exact, per D-01).
    - main.tsx renders StrictMode → HelmetProvider → WebsiteDataProvider → App.
    - App.tsx removes outer WebsiteDataProvider wrapper; keeps ThemeSynchronizer + Router inside App.
    - npm install completes without peer dependency errors on React 19.
  </acceptance_criteria>
  <action>
    Run npm install react-helmet-async@3.0.0. Import HelmetProvider from react-helmet-async in main.tsx. Lift WebsiteDataProvider from App.tsx into main.tsx per D-01 and 11-RESEARCH Pattern 2 (Helmet outside Router, data provider inside Helmet). App.tsx retains ThemeSynchronizer and Router only — single WebsiteDataProvider in tree.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>HelmetProvider and WebsiteDataProvider nest correctly; build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/seo core module</name>
  <read_first>
    - .planning/phases/BOOK-11-per-route-head-management/11-RESEARCH.md (resolvePageSeo — Implementation Notes)
    - .planning/phases/BOOK-11-per-route-head-management/PATTERNS.md
    - src/lib/websiteData.ts
  </read_first>
  <files>src/seo/types.ts, src/seo/routes.ts, src/seo/seoConfig.ts, src/seo/SeoHead.tsx, src/seo/usePageSeo.ts</files>
  <behavior>
    - resolvePageSeo on / with site title returns indexable PageSeo with absolute canonical when setSiteOrigin('https://example.com') called first.
    - /blog/:slug + published article: ogType article; title uses seoTitle ?? `${title} | ${site.title}` (D-06).
    - /blog/ghost + no article: robots noindex,nofollow; canonical absoluteUrl('/blog') (D-09).
    - article.noindex true OR !isPublished: robots noindex (D-15).
    - /admin/settings: robots noindex (D-13).
    - /community: robots noindex (D-14).
    - pathname /blog/foo/: canonical strips trailing slash (D-05).
    - googleSiteVerification present in settings.seo: included on indexable public routes only (D-19).
  </behavior>
  <acceptance_criteria>
    - types.ts exports PageSeo, ResolvePageSeoInput (pathname, data: WebsiteData, article?: Article).
    - routes.ts exports routeDefaults for /, /blog, /events, /community (unique titles per META-01), isAdminPath(pathname), isPublicMarketingPath helpers, PUBLIC_ROUTE_PATHS registry for Phase 13.
    - seoConfig.ts exports resolvePageSeo, normalizeCanonicalPath, resolveImageUrl — zero React imports (Phase 14 reuse).
    - resolvePageSeo implements D-04–D-15, D-19 branches per 11-RESEARCH route classification table; /events uses routeDefaults only (D-08).
    - ogImage fallback chain ends at absoluteUrl('/og-image.jpg') (D-06, D-12).
    - SeoHead.tsx maps PageSeo to Helmet: title, description, canonical, full og:* set, twitter:* with name= not property= (D-11), conditional robots and google-site-verification.
    - usePageSeo.ts accepts optional { article?: Article }; uses useLocation pathname + useWebsiteData; returns memoized PageSeo via resolvePageSeo.
  </acceptance_criteria>
  <action>
    Create src/seo/ per D-02. Implement pure resolver first, then SeoHead presenter, then usePageSeo hook. Seed routeDefaults copy from landing/blog/events/community section tone (planner discretion) — each static route title must differ from others. Admin/dashboard branch: title "Admin" or settings.seo.title; always robots noindex,nofollow; omit googleSiteVerification (D-19). Community: noindex but still emit title/description/OG for sharing (D-14, A3). Missing blog slug: noindex + canonical /blog, no article-shaped canonical (D-09). Never import useWebsiteData inside seoConfig.ts.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>seo module compiles; resolvePageSeo and SeoHead exported; no React in seoConfig.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Strip ThemeSynchronizer SEO, trim index.html, add default OG asset</name>
  <read_first>
    - src/App.tsx (ThemeSynchronizer lines 68-73)
    - index.html
    - .planning/phases/BOOK-11-per-route-head-management/11-CONTEXT.md (D-03, D-17, D-18)
  </read_first>
  <files>src/App.tsx, index.html, public/og-image.jpg</files>
  <acceptance_criteria>
    - ThemeSynchronizer useEffect no longer sets document.title or meta[name=description] (D-03); theme/CSS/customCss blocks unchanged.
    - index.html retains charset, viewport, favicon only from head extras; no og:*, twitter:*, link canonical, or meta name=title (D-17).
    - index.html title is neutral ("Superhumanly Monograph" or "Loading…"); optional single generic meta description not matching CMS marketing copy (D-18).
    - public/og-image.jpg exists (1200×630 recommended; minimal valid JPEG acceptable) for D-06 fallback.
    - grep index.html for monograph.superhumanly.ai returns 0 matches.
  </acceptance_criteria>
  <action>
    Delete App.tsx ThemeSynchronizer block 4 (lines 68-73 per CONTEXT). Trim index.html lines 8-28 duplicate SEO per 11-RESEARCH trim list. Add public/og-image.jpg — brand-appropriate placeholder if no design asset available. Do not add GSC meta to index.html (D-19).
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*#' index.html 2>/dev/null | grep -c 'og:' | grep -q '^0$' && grep -v '^[[:space:]]*#' index.html 2>/dev/null | grep -c 'rel="canonical"' | grep -q '^0$' && test -f public/og-image.jpg && npm run build</automated>
  </verify>
  <done>META-06 shell-only index.html; ThemeSynchronizer theme-only; default OG asset on disk.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` exits 0
- [ ] `document.querySelector` in ThemeSynchronizer grep shows no document.title assignment
- [ ] View Source on index.html shows no og:url or static canonical
</verification>

<success_criteria>
- HelmetProvider + src/seo/ module exist and compile
- index.html and ThemeSynchronizer no longer compete with per-route head
- public/og-image.jpg available for resolver fallback
</success_criteria>

<output>
Create `.planning/phases/BOOK-11-per-route-head-management/11-01-SUMMARY.md` when done
</output>
