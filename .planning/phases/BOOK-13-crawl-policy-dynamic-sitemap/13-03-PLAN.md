---
phase: 13-crawl-policy-dynamic-sitemap
plan: 03
type: execute
wave: 3
depends_on:
  - 13-02
files_modified:
  - server/src/routes/seoRoutes.ts
  - server/src/lib/crawlPolicy.ts
  - scripts/verify-phase13-crawl.mjs
  - .planning/phases/BOOK-13-crawl-policy-dynamic-sitemap/13-VALIDATION.md
requirements:
  - CRAWL-02
  - CRAWL-03
autonomous: true
must_haves:
  truths:
    - "prerender-paths API returns same paths as sitemap loc paths (minus origin)"
    - "Automated verify script passes against dev stack"
  artifacts:
    - path: server/src/routes/seoRoutes.ts
      provides: GET /api/v1/seo/prerender-paths
    - path: scripts/verify-phase13-crawl.mjs
      provides: Playwright/curl smoke for sitemap + robots
---

<objective>
Phase 14 handoff API, automated crawl verification, and Nyquist validation doc.

Purpose: D-12 prerender-paths + phase verification closure.
Output: prerender-paths endpoint, verify-phase13-crawl.mjs, finalized 13-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/BOOK-13-crawl-policy-dynamic-sitemap/13-02-SUMMARY.md
@scripts/verify-phase12-schema.mjs
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: prerender-paths API</name>
  <files>server/src/lib/crawlPolicy.ts, server/src/routes/seoRoutes.ts, server/src/index.ts</files>
  <action>
    Export `async function listIndexablePaths(prisma): Promise<string[]>` returning path strings (e.g. '/', '/blog', '/blog/slug', '/events') using same filters as sitemap entries.
    Mount `GET /api/v1/seo/prerender-paths` on content router OR seo sub-router under /api/v1/seo.
    Response: `{ paths: string[] }` JSON, no auth.
  </action>
  <verify>
    <automated>curl -s http://localhost:3001/api/v1/seo/prerender-paths | head -c 200</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Integration test prerender-paths vs sitemap</name>
  <files>server/src/lib/crawlPolicy.test.ts</files>
  <action>
    Add test: paths from listIndexablePaths match loc paths from listSitemapEntries (strip origin).
  </action>
  <verify>
    <automated>cd server && npm test</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: verify-phase13-crawl.mjs</name>
  <files>scripts/verify-phase13-crawl.mjs</files>
  <action>
    Node script (fetch or Playwright):
    1. GET {BASE}/sitemap.xml — parse XML, assert loc contains SITE_URL origin, assert at least one /blog/ slug if articles exist in API
    2. Assert no /admin or /community in locs
    3. GET {BASE}/robots.txt — assert Disallow: /admin, /dashboard, /community; Sitemap line matches origin
    4. Optional: compare prerender-paths count >= static routes (3)
    BASE defaults http://localhost:5173 (vite proxy) or UAT_BASE_URL env.
    Prerequisite comment: API + vite dev running.
  </action>
  <verify>
    <automated>node scripts/verify-phase13-crawl.mjs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Finalize VALIDATION.md</name>
  <files>.planning/phases/BOOK-13-crawl-policy-dynamic-sitemap/13-VALIDATION.md</files>
  <action>
    Set nyquist_compliant: true after all automated commands green.
    Map CRAWL-02 and CRAWL-03 to unit, integration, and e2e commands.
  </action>
  <verify>
    <automated>cd server && npm test && node ../scripts/verify-phase13-crawl.mjs</automated>
  </verify>
</task>

</tasks>
