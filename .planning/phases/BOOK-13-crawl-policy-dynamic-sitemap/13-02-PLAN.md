---
phase: 13-crawl-policy-dynamic-sitemap
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - server/src/routes/seoRoutes.ts
  - public/sitemap.xml
  - public/robots.txt
  - nginx.conf
  - vite.config.ts
requirements:
  - CRAWL-03
autonomous: true
must_haves:
  truths:
    - "GET /robots.txt disallows /admin and /dashboard"
    - "Sitemap line uses getSiteUrl() not hardcoded host"
    - "Stale public/sitemap.xml removed from frontend build"
    - "nginx proxies /sitemap.xml and /robots.txt to API"
  artifacts:
    - path: server/src/routes/seoRoutes.ts
      provides: GET /robots.txt dynamic handler
    - path: nginx.conf
      provides: Production crawl file routing
---

<objective>
Harden robots.txt and wire production/dev paths so crawlers hit the API, not stale static files.

Purpose: CRAWL-03 (D-06–D-11).
Output: Dynamic robots.txt, remove static sitemap, nginx + Vite proxy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/BOOK-13-crawl-policy-dynamic-sitemap/13-01-SUMMARY.md
@.planning/phases/BOOK-13-crawl-policy-dynamic-sitemap/13-CONTEXT.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Dynamic robots.txt route</name>
  <files>server/src/routes/seoRoutes.ts</files>
  <action>
    Add GET /robots.txt handler (D-07, D-14):
    ```
    User-agent: *
    Allow: /

    Disallow: /admin
    Disallow: /dashboard
    Disallow: /community

    Sitemap: ${getSiteUrl()}/sitemap.xml
    ```
    Content-Type text/plain; charset=utf-8. Cache-Control public max-age=3600.
  </action>
  <verify>
    <automated>curl -s http://localhost:3001/robots.txt</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Remove stale static crawl files</name>
  <files>public/sitemap.xml, public/robots.txt</files>
  <action>
    Delete public/sitemap.xml entirely.
    Delete public/robots.txt (API is authoritative). If Vite requires a file for dev without proxy, rely on vite proxy instead.
    Document in server/.env.example that SITE_URL must match public origin for Sitemap line.
  </action>
  <verify>
    <automated>npm run build && test ! -f public/sitemap.xml</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: nginx proxy for crawl files</name>
  <files>nginx.conf</files>
  <action>
    Add BEFORE `location /`:
    ```
    location = /sitemap.xml {
      proxy_pass http://book_api/sitemap.xml;
      ...
    }
    location = /robots.txt {
      proxy_pass http://book_api/robots.txt;
      ...
    }
    ```
    Same proxy headers as /api/ block.
  </action>
  <verify>
    <automated>grep -q 'sitemap.xml' nginx.conf && grep -q 'robots.txt' nginx.conf</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Vite dev proxy</name>
  <files>vite.config.ts</files>
  <action>
    Add server.proxy entries:
    '/sitemap.xml' → http://localhost:3001
    '/robots.txt' → http://localhost:3001
    So local SEO scripts can curl :5173/sitemap.xml during dev.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

</tasks>
