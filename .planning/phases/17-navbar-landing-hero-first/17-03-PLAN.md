---
phase: 17-navbar-landing-hero-first
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/components/sections/HeroSection.tsx
  - src/index.css
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05
  - HERO-06
must_haves:
  truths:
    - Hero h1 visible at first paint with sans typography
    - Primary Book Demo + secondary Community buttons replace action cards
    - No cursor spotlight or animated orbs
    - Video blob retained with stable dimensions
  artifacts:
    - src/components/sections/HeroSection.tsx
  key_links:
    - from: HeroSection
      to: LandingPage LeadCaptureModal
      via: onBookDemo prop
---

<objective>
LCP-safe, Stripe-style hero: sans headline, button pair, static background, refined split layout.

Purpose: Delivers HERO-01–HERO-06 without motion regression.
Output: Simplified HeroSection.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/17-navbar-landing-hero-first/17-CONTEXT.md
@src/components/sections/HeroSection.tsx
@src/pages/LandingPage.tsx
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip motion and mouse FX</name>
  <files>src/components/sections/HeroSection.tsx</files>
  <read_first>src/components/sections/HeroSection.tsx</read_first>
  <action>
Remove:
- `useMotionValue`, `useSpring`, mousemove listener
- Cursor spotlight motion.div
- Animated ambient orbs (infinite y/x loops)
- Stagger `variants` / `initial="hidden"` on content wrapper
- `whileHover` on cards (cards will be removed)

Remove framer-motion import entirely if no longer needed.

Keep `usePrefersReducedMotion` only if still used for optional subtle CSS; otherwise remove.

Background: rely on static `hero-aura-bg` + `hero-noise` classes only.
  </action>
  <verify>npm run build && rg "framer-motion|useMotionValue|mouseX" src/components/sections/HeroSection.tsx || echo "clean"</verify>
  <acceptance_criteria>
    - HeroSection has no framer-motion import
    - h1 renders without opacity-0 initial state
  </acceptance_criteria>
  <done>Hero is static-first for LCP</done>
</task>

<task type="auto">
  <name>Task 2: Sans typography and content hierarchy</name>
  <files>src/components/sections/HeroSection.tsx</files>
  <read_first>17-CONTEXT.md</read_first>
  <action>
Update h1 classes: remove `font-serif`, use sans display scale e.g.:
`text-[clamp(2rem,5vw,4.5rem)] font-bold tracking-tight leading-[1.05] text-text`

Keep CMS fields: tagline, headline, headlineAccent (accent span can stay italic sans or semibold accent color).

Subtitle: readable `text-muted max-w-xl`.

Section height: `min-h-0 lg:min-h-screen` — content-driven on mobile per D-16; adjust pt to account for fixed nav (`pt-24 lg:pt-28`).
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - h1 uses font-sans or default sans stack, not font-serif
  </acceptance_criteria>
  <done>Sans hero hierarchy applied</done>
</task>

<task type="auto">
  <name>Task 3: Replace action cards with button pair</name>
  <files>src/components/sections/HeroSection.tsx</files>
  <read_first>src/index.css</read_first>
  <action>
Remove `actionItems` glass card map.

Add flex row/col button group:
1. **Primary** — "Book a Demo" (or CMS-friendly label) — `type="button"` calls `onBookDemo()`; classes `btn-cta-primary min-h-11 px-8`
2. **Secondary** — "Join Founder Network" — `<a href="/community">` with `btn-cta-secondary min-h-11 px-8`

Keep `MarketingService.logEvent` on clicks.

Both buttons: `focus-visible:ring-2` for a11y.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - No glass action card markup remains
    - Primary triggers onBookDemo; secondary links to /community
  </acceptance_criteria>
  <done>Stripe-style CTA pair in hero</done>
</task>

<task type="auto">
  <name>Task 4: Video column CLS and polish</name>
  <files>src/components/sections/HeroSection.tsx</files>
  <read_first>src/components/sections/HeroSection.tsx</read_first>
  <action>
Right column: keep play-thumbnail + iframe pattern.

Wrap video container with explicit aspect ratio e.g. `aspect-[16/10]` or fixed min-heights per breakpoint to prevent CLS.

Polish glow-border container; remove scale pulse animation on backdrop blob — static gradient blur acceptable.

Ensure decorative layers use `pointer-events-none` and don't cover text column.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Video wrapper has aspect-ratio or explicit height classes
  </acceptance_criteria>
  <done>Stable hero media column</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passes
- [ ] Hero h1 visible in SSR/prerender HTML without opacity:0
- [ ] Lighthouse spot-check: LCP element is headline text
</verification>

<success_criteria>
- Hero matches CONTEXT decisions (sans, buttons, static, video blob)
- HERO requirements satisfied
</success_criteria>
