---
phase: 17-navbar-landing-hero-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/websiteData.ts
  - src/components/WebsiteDataProvider.tsx
  - src/components/admin/SettingsManager.tsx
autonomous: true
requirements:
  - NAV-01
must_haves:
  truths:
    - Editor can set header primary CTA label and href in admin Navigation tab
    - Navbar reads primaryCta from CMS data with sensible defaults
  artifacts:
    - src/lib/websiteData.ts
    - src/components/admin/SettingsManager.tsx
  key_links:
    - from: src/components/Navbar.tsx
      to: settings.navigation.primaryCta
      via: useWebsiteData()
---

<objective>
Add CMS-driven primary navigation CTA fields and admin editing so the header no longer hardcodes "Join Now".

Purpose: Unlocks D-21 and Stripe-like configurable marketing CTA.
Output: Typed `primaryCta` on navigation settings with admin UI and merged defaults.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/17-navbar-landing-hero-first/17-CONTEXT.md
@.planning/phases/17-navbar-landing-hero-first/17-RESEARCH.md
@src/lib/websiteData.ts
@src/components/admin/SettingsManager.tsx
@src/components/WebsiteDataProvider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend navigation types and defaults</name>
  <files>src/lib/websiteData.ts</files>
  <read_first>src/lib/websiteData.ts</read_first>
  <action>
Add to `SiteSettings.navigation`:
```typescript
primaryCta: { label: string; href: string };
```
In `initialData.settings.navigation`, set defaults:
```typescript
primaryCta: { label: 'Join Now', href: '/#final-cta' }
```
Ensure TypeScript compiles; export type if NavLink pattern used elsewhere.
  </action>
  <verify>npm run build 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - websiteData.ts contains `primaryCta` in navigation interface and initialData
  </acceptance_criteria>
  <done>Types and defaults exist for primaryCta</done>
</task>

<task type="auto">
  <name>Task 2: Merge remote primaryCta in provider</name>
  <files>src/components/WebsiteDataProvider.tsx</files>
  <read_first>src/components/WebsiteDataProvider.tsx</read_first>
  <action>
When merging `remoteData.settings.navigation`, deep-merge `primaryCta` with defaults so partial API payloads don't wipe label/href.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Provider merge includes primaryCta fallback to initialData defaults
  </acceptance_criteria>
  <done>Hydration preserves primaryCta</done>
</task>

<task type="auto">
  <name>Task 3: Admin Navigation tab — Primary CTA fields</name>
  <files>src/components/admin/SettingsManager.tsx</files>
  <read_first>src/components/admin/SettingsManager.tsx</read_first>
  <action>
In navigation tab UI (above or below main links list), add two inputs:
- Primary CTA Label (text)
- Primary CTA URL (text, placeholder `/#final-cta`)

Wire to `form.navigation.primaryCta.label` and `.href`. Match existing form styling (Input components / admin patterns).

Add helper microcopy: "Shown as the pill button in the site header on desktop and mobile menu."
  </action>
  <verify>npm run build && npm run lint 2>&1 | tail -30</verify>
  <acceptance_criteria>
    - SettingsManager navigation tab renders primaryCta label and href inputs
    - Saving settings persists primaryCta in form state
  </acceptance_criteria>
  <done>Editors can configure header CTA in admin</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passes
- [ ] Grep confirms `primaryCta` in websiteData.ts and SettingsManager.tsx
</verification>

<success_criteria>
- CMS schema + admin UI for header primary CTA complete
- No hardcoded Join Now dependency in types layer
</success_criteria>
