---
phase: 17-navbar-landing-hero-first
plan: 04
type: execute
wave: 3
depends_on: ["17-02", "17-03"]
files_modified:
  - src/index.css
  - package.json
autonomous: true
requirements:
  - NAV-02
  - NAV-03
  - HERO-02
  - HERO-04
  - HERO-06
must_haves:
  truths:
    - Build and lint pass after phase changes
    - Responsive sanity at key breakpoints documented
  artifacts:
    - scripts/verify-phase17-nav-hero.mjs
  key_links: []
---

<objective>
Verify Phase 17 navbar and hero meet requirements: build gates, grep checks, basic responsive assertions.

Purpose: Close the phase with automated checks executors can re-run.
Output: verify script + passing build/lint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/17-navbar-landing-hero-first/17-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add verify script</name>
  <files>scripts/verify-phase17-nav-hero.mjs</files>
  <read_first>scripts/verify-phase14-prerender.mjs</read_first>
  <action>
Create `scripts/verify-phase17-nav-hero.mjs` that:
1. Reads `src/components/Navbar.tsx` — assert NO `framer-motion`, NO `ContactSupportModal`, YES `primaryCta`
2. Reads `src/components/sections/HeroSection.tsx` — assert NO `framer-motion`, NO `useMotionValue`, h1 does NOT contain `opacity: 0` or `initial="hidden"` on h1
3. Reads `src/index.css` — assert `scroll-padding-top` present
4. Reads `src/lib/websiteData.ts` — assert `primaryCta` in navigation type
5. Exit 0 on pass, exit 1 with message on fail

Add npm script `"verify:phase17": "node scripts/verify-phase17-nav-hero.mjs"` to package.json if verify scripts pattern exists.
  </action>
  <verify>node scripts/verify-phase17-nav-hero.mjs</verify>
  <acceptance_criteria>
    - Script exists and runs
  </acceptance_criteria>
  <done>Automated phase verify in place</done>
</task>

<task type="auto">
  <name>Task 2: Full build and lint gate</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
Run `npm run lint` and `npm run build` from repo root. Fix any errors introduced by plans 01–03.
  </action>
  <verify>npm run lint && npm run build && node scripts/verify-phase17-nav-hero.mjs</verify>
  <acceptance_criteria>
    - lint and build exit 0
    - verify script exit 0
  </acceptance_criteria>
  <done>Phase 17 passes automated gates</done>
</task>

</tasks>

<verification>
- [ ] All verify checks green
- [ ] No framer-motion in Navbar or HeroSection
</verification>

<success_criteria>
- Phase 17 ready for human visual review and execute-phase summary
</success_criteria>
