---
phase: 17-navbar-landing-hero-first
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/components/Navbar.tsx
  - src/index.css
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
  - NAV-06
must_haves:
  truths:
    - Desktop header shows refined glass pill with CMS primary CTA
    - Mobile menu has 44px trigger, focus trap, sans link typography
    - No Framer Motion on navbar
    - Anchor links not hidden under sticky header
  artifacts:
    - src/components/Navbar.tsx
    - src/index.css
  key_links:
    - from: Navbar
      to: settings.navigation.primaryCta
      via: useWebsiteData()
---

<objective>
Premium Stripe-polished navbar with accessible mobile menu, CMS CTA, CSS-only motion.

Purpose: Delivers NAV-01–NAV-06 and visual quality bar for sitewide nav.
Output: Refactored Navbar.tsx + supporting CSS utilities.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/17-navbar-landing-hero-first/17-CONTEXT.md
@src/components/Navbar.tsx
@src/index.css
@src/components/ui/Button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Framer Motion and Support AI from Navbar</name>
  <files>src/components/Navbar.tsx</files>
  <read_first>src/components/Navbar.tsx</read_first>
  <action>
- Remove imports: `motion`, `AnimatePresence` from framer-motion
- Remove `ContactSupportModal` import, state, and mobile Support AI button
- Replace `motion.div` wrapper with plain `div`
- Mobile sheet: use CSS `transition-opacity` + conditional render OR Radix Dialog if already in project for focus trap

Keep `isInsidePreview` fixed vs absolute behavior unchanged.
  </action>
  <verify>npm run build && rg "framer-motion" src/components/Navbar.tsx || echo "clean"</verify>
  <acceptance_criteria>
    - Navbar.tsx does not import framer-motion
    - No ContactSupportModal in Navbar.tsx
  </acceptance_criteria>
  <done>Navbar free of framer and Support AI</done>
</task>

<task type="auto">
  <name>Task 2: Wire CMS primaryCta and refine desktop nav</name>
  <files>src/components/Navbar.tsx</files>
  <read_first>src/lib/websiteData.ts</read_first>
  <action>
Read `settings.navigation.primaryCta` from useWebsiteData().

Replace hardcoded "Join Now" Link with dynamic label/href:
- Resolve href like existing `getHRef` for hash routes
- CTA classes: `min-h-11 px-5 lg:px-7 bg-accent text-white rounded-full font-bold shadow-elite hover:bg-accent2 focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2`

Refine nav link classes: consistent `min-h-11` tap area, `focus-visible:ring-2`.

Keep social icons inline on desktop per D-08.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Navbar uses primaryCta.label and primaryCta.href
    - No string literal "Join Now" hardcoded in Navbar (except fallback if needed in merge layer only)
  </acceptance_criteria>
  <done>CMS-driven header CTA live</done>
</task>

<task type="auto">
  <name>Task 3: Mobile menu — sans typography, a11y, dual CTA</name>
  <files>src/components/Navbar.tsx</files>
  <read_first>17-CONTEXT.md</read_first>
  <action>
Mobile overlay sheet (below pill):
- Link typography: `text-lg font-sans font-medium` (~18–20px), NOT serif 2xl–4xl
- Menu button: `min-h-11 min-w-11`, aria-expanded, aria-controls
- Implement focus trap (Radix Dialog recommended) + Escape closes + body overflow hidden
- Bottom: single primary CTA from CMS (same as desktop) — no Support AI second button
- Social row retained

Respect `prefers-reduced-motion`: no y-translate animation on menu; opacity-only or instant.
  </action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - Mobile links use sans at modest scale
    - Menu trigger meets 44px minimum
  </acceptance_criteria>
  <done>Mobile nav accessible and premium</done>
</task>

<task type="auto">
  <name>Task 4: CSS — glass-pill polish and scroll-padding</name>
  <files>src/index.css</files>
  <read_first>src/index.css</read_first>
  <action>
Refine `.glass-pill` / `.shadow-nav`: tighter padding vars, subtle border, backdrop-blur with `@media (prefers-reduced-motion: reduce)` disabling transitions.

Add to `:root` or `html`:
```css
scroll-padding-top: 5.5rem; /* tune to fixed header height */
```

Optional `--header-offset` token for consistency.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - index.css contains scroll-padding-top
    - glass-pill utility updated
  </acceptance_criteria>
  <done>Global anchor offset and pill polish</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` and `npm run lint` pass
- [ ] Manual: mobile menu keyboard tab cycle works
- [ ] Viewport 320px: no horizontal scroll on header
</verification>

<success_criteria>
- NAV requirements addressable via navbar implementation
- Stripe-like refined pill without framer
</success_criteria>
