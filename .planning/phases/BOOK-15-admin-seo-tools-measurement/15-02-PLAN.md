---
phase: 15-admin-seo-tools-measurement
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - server/package.json
  - server/package-lock.json
  - server/src/routes/adminRoutes.ts
  - server/src/routes/adminOgImage.test.ts
  - src/lib/api.ts
  - src/components/admin/OgImageUpload.tsx
  - src/components/admin/ArticleSeoTab.tsx
  - src/components/admin/SettingsManager.tsx
  - public/og/.gitkeep
requirements:
  - CMS-05
autonomous: true
user_setup: []
must_haves:
  truths:
    - "POST /api/v1/admin/og-image requires Bearer admin token"
    - "Upload accepts jpeg/png/webp max 5MB; returns /og/{uuid}.jpg at 1200x630"
    - "Article SEO tab and Settings SEO tab have upload + manual URL field"
    - "Successful upload sets ogImage URL and refreshes thumbnail"
  artifacts:
    - path: server/src/routes/adminRoutes.ts
      provides: POST /og-image handler
    - path: src/components/admin/OgImageUpload.tsx
      provides: Client upload control
    - path: public/og/
      provides: Served OG assets
  key_links:
    - from: src/components/admin/OgImageUpload.tsx
      to: server/src/routes/adminRoutes.ts
      via: multipart POST with Authorization Bearer
      pattern: "admin/og-image"
    - from: server/src/routes/adminRoutes.ts
      to: public/og/
      via: sharp writeFile
      pattern: "public/og"
---

<objective>
Server-side OG image resize/upload and admin upload controls on article and settings SEO surfaces.

Purpose: CMS-05 (D-36–D-39).
Output: sharp + multer route, OgImageUpload component, api.uploadOgImage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/BOOK-15-admin-seo-tools-measurement/15-CONTEXT.md
@.planning/phases/BOOK-15-admin-seo-tools-measurement/15-RESEARCH.md
@.planning/research/STACK.md
</execution_context>

<context>
@server/src/routes/adminRoutes.ts
@server/src/index.ts
@src/lib/api.ts
@src/components/admin/ArticleSeoTab.tsx
@src/components/admin/SettingsManager.tsx
@src/pages/AdminPage.tsx

<interfaces>
authenticateAdmin middleware on adminRoutes — all routes after router.use(authenticateAdmin).

Admin token: localStorage adminToken; api pattern Authorization: Bearer ${token}.

ArticleSeoTab from 15-01: ogImage string field + thumbnail preview.
</interfaces>
</context>

<threat_model>
| ID | Threat | Disposition | Mitigation |
|----|--------|-------------|------------|
| T-15-03 | Unauthenticated OG upload | mitigate | Route behind authenticateAdmin |
| T-15-04 | Malicious file upload | mitigate | MIME allowlist, 5MB limit, sharp re-encode |
| T-15-05 | Path traversal in filename | mitigate | UUID filename only; write only under public/og/ |
| T-15-SC | Supply chain (sharp, multer) | mitigate | RESEARCH Package Legitimacy Audit [OK] |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: POST /api/v1/admin/og-image with sharp</name>
  <files>server/package.json, server/src/routes/adminRoutes.ts, server/src/routes/adminOgImage.test.ts, public/og/.gitkeep</files>
  <action>
    Per D-36: In server/, npm install sharp@0.34.5 multer@2.0.2.

    Add POST /og-image on adminRoutes (path mounts as /api/v1/admin/og-image). multer memoryStorage, field name file, limits fileSize 5*1024*1024.

    Validate mimetype in image/jpeg, image/png, image/webp — else 400 JSON error.

    sharp: resize 1200x630 fit cover position centre, output JPEG quality 85. Write to repo-root public/og/{uuid}.jpg (resolve repo root from server — document in comment if using path.join(__dirname, '../../public/og')).

    Response 200 { url: "/og/{filename}" }. Errors 400/413/500 with message.

  Vitest adminOgImage.test.ts: 401 without Authorization; 400 invalid mimetype (mock buffer); optional success with sharp mocked.

    Ensure public/og/ exists (.gitkeep). Uploaded files may be gitignored via public/og/*.jpg pattern if desired (discretion).
  </action>
  <verify>
    <automated>cd server && npm test -- adminOgImage</automated>
  </verify>
  <done>Authenticated POST returns /og/ URL; rejects oversize and wrong MIME.</done>
</task>

<task type="auto">
  <name>Task 2: OgImageUpload client on article and settings SEO</name>
  <files>src/lib/api.ts, src/components/admin/OgImageUpload.tsx, src/components/admin/ArticleSeoTab.tsx, src/components/admin/SettingsManager.tsx</files>
  <action>
    Per D-37–D-39: api.uploadOgImage(token, file: File) — FormData append file, POST ${API_BASE}/admin/og-image, Authorization Bearer, return parsed { url }.

    OgImageUpload props: value (ogImage url), onChange(url), getToken (): string. Client validation before upload: max 5MB, accept image/jpeg image/png image/webp. Show progress (disabled button + Loader2) and error via toast or inline alert on failure.

    On success: onChange(returned url); thumbnail refreshes from new url.

    Wire into ArticleSeoTab above or beside ogImage URL input — retain manual URL input per D-37.

    Wire into SettingsManager SEO tab for settings.seo.ogImage — replace "Social preview in admin arrives in a later release" help with upload + URL. Upload supplements default og-image.jpg when set (discretion D-36 settings scope).

    Use same adminToken as WebsiteDataProvider / AdminPage.
  </action>
  <verify>
    <automated>npm run build:no-prerender</automated>
  </verify>
  <done>Both surfaces upload image, set ogImage field, show 1200x630 thumbnail from /og/ path.</done>
</task>

</tasks>

<verification>
- Manual: Login admin → article SEO tab → upload PNG → field shows /og/*.jpg → image loads at http://localhost:5173/og/... in dev.
- cd server && npm test passes.
</verification>

<success_criteria>
- CMS-05: Server resizes to 1200×630; editors upload without external image tooling.
</success_criteria>

<output>
Create .planning/phases/BOOK-15-admin-seo-tools-measurement/15-02-SUMMARY.md when done
</output>
