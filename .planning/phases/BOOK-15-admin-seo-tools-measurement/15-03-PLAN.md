---
phase: 15-admin-seo-tools-measurement
plan: 03
type: execute
wave: 3
depends_on: [15-01]
files_modified:
  - package.json
  - package-lock.json
  - src/lib/reportWebVitals.ts
  - src/lib/reportWebVitals.test.ts
  - src/App.tsx
  - src/components/admin/SettingsManager.tsx
  - scripts/verify-phase15-gsc.mjs
requirements:
  - MSMT-01
  - PERF-04
autonomous: true
must_haves:
  truths:
    - "Production only: LCP, INP, CLS fire web_vital events via MarketingService"
    - "Single web-vitals listener survives HMR (module guard)"
    - "Settings GSC help documents save + build + View Source on prerendered /"
    - "verify-phase15-gsc.mjs greps google-site-verification in dist/index.html"
  artifacts:
    - path: src/lib/reportWebVitals.ts
      provides: initWebVitalsReporting
      exports: ["initWebVitalsReporting"]
    - path: scripts/verify-phase15-gsc.mjs
      provides: MSMT-01 prerender proof
  key_links:
    - from: src/lib/reportWebVitals.ts
      to: src/lib/marketing.ts
      via: MarketingService.logEvent('web_vital', ...)
      pattern: "web_vital"
    - from: src/App.tsx
      to: src/lib/reportWebVitals.ts
      via: MarketingTracker useEffect
      pattern: "initWebVitalsReporting"
---

<objective>
Production Core Web Vitals RUM through existing marketing webhook and automated GSC meta verification on prerendered HTML.

Purpose: PERF-04, MSMT-01 (D-40–D-44, D-41).
Output: reportWebVitals.ts, verify-phase15-gsc.mjs, SettingsManager GSC help update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/BOOK-15-admin-seo-tools-measurement/15-CONTEXT.md
@.planning/phases/BOOK-15-admin-seo-tools-measurement/15-RESEARCH.md
@.planning/phases/BOOK-14-build-time-prerender/14-02-PLAN.md
</execution_context>

<context>
@src/lib/marketing.ts
@src/App.tsx
@src/seo/SeoHead.tsx
@src/components/admin/SettingsManager.tsx
@scripts/verify-phase14-prerender.mjs

<interfaces>
MarketingService.logEvent(action: string, metadata: Record&lt;string, unknown&gt;): Promise&lt;void&gt;
Posts to ${API_BASE}/marketing/webhook with body.type = action.

SeoHead already renders google-site-verification when seo.googleSiteVerification set (Phase 11 D-19).
</interfaces>
</context>

<threat_model>
| ID | Threat | Disposition | Mitigation |
|----|--------|-------------|------------|
| T-15-06 | PII in web_vital payloads | accept | path + metrics only; no email in web_vital metadata |
| T-15-SC | web-vitals package | mitigate | RESEARCH audit [OK] googlechrome/web-vitals |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: web-vitals RUM via MarketingService</name>
  <files>package.json, src/lib/reportWebVitals.ts, src/lib/reportWebVitals.test.ts, src/App.tsx</files>
  <action>
    Per D-42: npm install web-vitals@5.2.0 at repo root.

    Per D-43–D-44: Create src/lib/reportWebVitals.ts — export initWebVitalsReporting(). If import.meta.env.PROD is false, return immediately. Module-level started flag: if started return; else set started true. Register onLCP, onINP, onCLS callbacks; each calls MarketingService.logEvent('web_vital', { name, value, rating, delta, path: window.location.pathname }).

    Import initWebVitalsReporting in MarketingTracker (src/App.tsx) inside existing useEffect with [] alongside MarketingService.init() — do not duplicate in App root.

    reportWebVitals.test.ts: vi.stubEnv or mock import.meta.env.PROD false — assert onLCP not called / no logEvent; PROD true mock logEvent and assert called with web_vital.

    Do NOT add /api/v1/marketing/events route — use existing webhook only.
  </action>
  <verify>
    <automated>npm test -- reportWebVitals</automated>
  </verify>
  <done>Tests pass; dev build does not register listeners; PROD path sends web_vital events.</done>
</task>

<task type="auto">
  <name>Task 2: GSC admin help and verify-phase15-gsc.mjs</name>
  <files>src/components/admin/SettingsManager.tsx, scripts/verify-phase15-gsc.mjs, package.json</files>
  <action>
    Per D-40: Update SettingsManager #settings-gsc-help copy — token is injected on public pages via SeoHead; after saving settings run npm run build (with API on :3001) so prerender bakes meta into dist/; confirm in View Source on / for meta name=google-site-verification content={token}. Remove any "later phase" wording.

    Per D-41: Add scripts/verify-phase15-gsc.mjs modeled on verify-phase14-prerender.mjs:
    - Require dist/index.html exists (exit 2 if missing with message to run build).
    - Read token from env GSC_TEST_TOKEN (required) or fail with instruction to set from seed/settings.
    - Grep html for google-site-verification meta with content matching token (support name/content attribute order variants like phase14 helpers).
    - Exit 0 on pass, 1 on fail; console pass/fail lines.

    Add npm script verify:phase15-gsc: node scripts/verify-phase15-gsc.mjs in package.json scripts.
  </action>
  <verify>
    <automated>node -c scripts/verify-phase15-gsc.mjs</automated>
  </verify>
  <done>Help text updated; script syntax valid; document GSC_TEST_TOKEN in script header comment.</done>
</task>

</tasks>

<verification>
- PERF-04: Production build preview — Network tab shows POST /api/v1/marketing/webhook with type web_vital after interaction (or document manual check in SUMMARY).
- MSMT-01: Set googleSiteVerification in admin → npm run build → GSC_TEST_TOKEN=xxx npm run verify:phase15-gsc exits 0.
</verification>

<success_criteria>
- PERF-04: LCP, INP, CLS reported in production via existing telemetry.
- MSMT-01: Verification meta provable in prerendered dist/index.html.
</success_criteria>

<output>
Create .planning/phases/BOOK-15-admin-seo-tools-measurement/15-03-SUMMARY.md when done
</output>
