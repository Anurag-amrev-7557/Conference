---
phase: 15-admin-seo-tools-measurement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/BlogManager.tsx
  - src/components/admin/ArticleSeoTab.tsx
  - src/components/admin/SeoPreviewPanel.tsx
  - src/hooks/useDraftArticleSeo.ts
requirements:
  - CMS-03
  - CMS-04
autonomous: true
must_haves:
  truths:
    - "Article editor shows Content | SEO tabs; SEO fields not in Content scroll"
    - "SEO tab shows 70/160 char counters and noindex toggle"
    - "Read-only resolved row shows title, description, image with fallback source labels"
    - "SERP and social mocks update from resolvePageSeo on draft edits (~300ms debounce)"
    - "Previews match resolvePageSeo for same draft article and /blog/{slug} pathname"
  artifacts:
    - path: src/hooks/useDraftArticleSeo.ts
      provides: Draft resolvePageSeo + fallback labels
      exports: ["useDraftArticleSeo"]
    - path: src/components/admin/SeoPreviewPanel.tsx
      provides: SERP + social static mocks
    - path: src/components/admin/ArticleSeoTab.tsx
      provides: SEO tab fields and resolved row
  key_links:
    - from: src/hooks/useDraftArticleSeo.ts
      to: src/seo/seoConfig.ts
      via: resolvePageSeo import
      pattern: "resolvePageSeo\\("
    - from: src/components/admin/SeoPreviewPanel.tsx
      to: src/hooks/useDraftArticleSeo.ts
      via: PageSeo props
      pattern: "useDraftArticleSeo|seo\\."
---

<objective>
Dedicated article SEO tab with live SERP and social previews driven by resolvePageSeo (no duplicate preview logic).

Purpose: CMS-03, CMS-04 (D-28–D-35, D-32).
Output: ArticleSeoTab, SeoPreviewPanel, useDraftArticleSeo; BlogManager tab refactor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/BOOK-15-admin-seo-tools-measurement/15-CONTEXT.md
@.planning/phases/BOOK-15-admin-seo-tools-measurement/15-RESEARCH.md
@.planning/phases/BOOK-10-seo-data-model-site-url-contract/10-UI-SPEC.md
</execution_context>

<context>
@src/seo/seoConfig.ts
@src/seo/types.ts
@src/components/admin/BlogManager.tsx
@src/components/WebsiteDataProvider.tsx
@src/lib/websiteData.ts

<interfaces>
From src/seo/seoConfig.ts:
- resolvePageSeo({ pathname, data, article }: ResolvePageSeoInput): PageSeo
- articleSeo uses: seoTitle ?? `${title} | ${site.title}`; seoDescription ?? excerpt ?? site.description; ogImage ?? site.ogImage

From src/seo/types.ts:
- PageSeo: title, description, canonical, ogType, ogImage, ogUrl, robots?, googleSiteVerification?

BlogManager state: editingId, editForm: Partial&lt;Article&gt;, useWebsiteData().data
</interfaces>
</context>

<threat_model>
| ID | Threat | Disposition | Mitigation |
|----|--------|-------------|------------|
| T-15-01 | XSS via SEO strings in preview DOM | mitigate | Text nodes only in mocks; no dangerouslySetInnerHTML |
| T-15-02 | Preview diverges from live head | mitigate | Single resolvePageSeo import per D-32 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: useDraftArticleSeo hook and ArticleSeoTab shell</name>
  <files>src/hooks/useDraftArticleSeo.ts, src/components/admin/ArticleSeoTab.tsx, src/components/admin/BlogManager.tsx</files>
  <action>
    Per D-28: Add segmented tabs **Content** | **SEO** at top of article editor (editingId view only). Content tab keeps title, slug, category, excerpt, cover, markdown, publish — remove inline Search &amp; Social block (~lines 229–305).

    Per D-29: ArticleSeoTab fields — seoTitle, seoDescription, ogImage URL, noindex toggle (existing bindings). Soft limits with live counters text-[10px] text-muted: NN/70 for title, NN/160 for description.

    Implement useDraftArticleSeo(editForm, articleId, data): merge editForm over stored article for synthetic Article; pathname `/blog/${slug}` when slug present else `/blog/preview`; call resolvePageSeo({ pathname, data, article: synthetic }). Return { seo: PageSeo, fallbackLabels: { title, description, image } } per D-30 by comparing which override fields were empty against articleSeo fallback chain in seoConfig.ts.

    Per D-31: Help paragraph under tab heading — previews and live site use the same resolver as public pages (seoConfig.ts).

    Per D-30: Read-only "Resolved for live site" row — title, description, thumbnail (ogImage), each with small muted source label when fallback applied.

    Match admin input classes from BlogManager: bg-[#fafafa] border border-border/40 rounded-xl.
  </action>
  <verify>
    <automated>npm test -- src/seo/seoConfig.test.ts</automated>
  </verify>
  <done>SEO tab visible; Content tab has no SEO fields; resolved row shows live resolvePageSeo output with fallback labels.</done>
</task>

<task type="auto">
  <name>Task 2: SeoPreviewPanel SERP and social mocks</name>
  <files>src/components/admin/SeoPreviewPanel.tsx, src/components/admin/ArticleSeoTab.tsx</files>
  <action>
    Per D-32–D-34: SeoPreviewPanel accepts PageSeo from useDraftArticleSeo (debounced editForm ~300ms for preview only; counters stay instant).

    SERP mock (static): blue link title (seo.title), green URL line from hostname of seo.canonical (new URL(seo.canonical).hostname), gray description (seo.description). Max-width ~600px card on white/off background.

    Social mock: summary_large_image card ~1.91:1 image area (seo.ogImage), below show og:title (seo.title), description, hostname from canonical. Use border-border/40 rounded-xl to match admin.

    Per D-35: If slug missing or title empty, show placeholder state ("Add a URL slug to preview" / muted empty state) instead of broken layout.

    Wire SeoPreviewPanel into ArticleSeoTab below resolved row. No iframe, no external widgets.
  </action>
  <verify>
    <automated>npm run build:no-prerender</automated>
  </verify>
  <done>SERP and social cards render; typing seoTitle updates debounced preview; empty slug shows placeholder.</done>
</task>

</tasks>

<verification>
- Manual: Admin → Blog → edit article → SEO tab → change seoTitle and confirm SERP title matches View Source on public /blog/{slug} for saved article with same fields.
- npm test passes; build:no-prerender succeeds.
</verification>

<success_criteria>
- CMS-03: Dedicated SEO tab with fallback chain visibility.
- CMS-04: Admin previews use resolvePageSeo only; visually match live meta for same inputs.
</success_criteria>

<output>
Create .planning/phases/BOOK-15-admin-seo-tools-measurement/15-01-SUMMARY.md when done
</output>
