---
phase: 01-backend-completeness
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/contentRoutes.ts
  - /Users/anuragverma/Downloads/book website-frontend/src/lib/api.ts
  - /Users/anuragverma/Downloads/book website-frontend/src/components/WebsiteDataProvider.tsx
  - /Users/anuragverma/Downloads/book website-frontend/src/lib/websiteData.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/seed.ts
requirements:
  - BACK-06
  - BACK-07
autonomous: true
must_haves:
  truths:
    - "D-15: Plans 01-01/02 kept monolithic GET /content; 01-03 adds pagination/split with limit/offset."
    - "D-16: WebsiteDataProvider loads split content via Promise.all and merges WebsiteData."
    - "D-17: seed.ts authoritative; websiteData.ts initialData skeleton-only per file header."
  artifacts:
    - path: /Users/anuragverma/Downloads/book website-frontend/src/lib/websiteData.ts
      provides: Skeleton-only offline defaults
    - path: /Users/anuragverma/Downloads/book website-frontend/server/src/routes/contentRoutes.ts
      provides: Paginated or split read API
  key_links:
    - from: /Users/anuragverma/Downloads/book website-frontend/src/components/WebsiteDataProvider.tsx
      to: /Users/anuragverma/Downloads/book website-frontend/server/src/routes/contentRoutes.ts
      via: Promise.all parallel getContent* calls
      pattern: Promise.all
---

<objective>
Refine content API shape and eliminate default-content drift between seed and frontend skeleton.

Purpose: Deliver BACK-06 and BACK-07.
Output: Paginated or split content endpoints, parallel provider fetch, skeleton initialData.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-backend-completeness/01-CONTEXT.md
@.planning/phases/01-backend-completeness/01-RESEARCH.md
</execution_context>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| — | No new auth surface in this plan | — | Read-only public endpoints only |
</threat_model>

<context>
Locked: D-15 keep monolithic for 01-01/02 work; 01-03 adds pagination OR split. D-16 parallel Promise.all. D-17 seed authoritative.
Choose split endpoints (cleaner for BACK-07): GET /content/site, /content/articles, /content/events, /content/community with optional limit/offset query params on list endpoints.
Keep GET /content as deprecated alias merging all four for backward compatibility OR remove after provider migrated (prefer migrate provider then keep /content as thin aggregator calling same helpers).
Default pagination: limit=50 offset=0 on articles/events/community lists.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split content read API with pagination params</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/contentRoutes.ts
    - /Users/anuragverma/Downloads/book website-frontend/server/src/seed.ts
    - /Users/anuragverma/Downloads/book website-frontend/.planning/phases/01-backend-completeness/01-CONTEXT.md
  </read_first>
  <files>server/src/routes/contentRoutes.ts, server/src/index.ts</files>
  <acceptance_criteria>
    - contentRoutes exposes GET /site (hero, settings, appearance, stats, pillars, perks), GET /articles?limit=&offset=, GET /events?limit=&offset=, GET /community?limit=&offset=.
    - GET / (legacy monolithic) still works for backward compat OR returns 301 note in comment — provider must not depend on it after Task 2.
    - List endpoints default limit=50 offset=0 and return { items, total } or items array with X-Total-Count header.
  </acceptance_criteria>
  <action>Refactor contentRoutes into shared query helpers. Add pagination parsing from req.query. Mount sub-routes or single router with multiple paths under /api/v1/content.</action>
</task>

<task type="auto">
  <name>Task 2: WebsiteDataProvider parallel fetch</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/src/components/WebsiteDataProvider.tsx
    - /Users/anuragverma/Downloads/book website-frontend/src/lib/api.ts
    - /Users/anuragverma/Downloads/book website-frontend/src/lib/websiteData.ts
  </read_first>
  <files>src/lib/api.ts, src/components/WebsiteDataProvider.tsx</files>
  <acceptance_criteria>
    - api.ts has getContentSite, getArticles, getEvents, getCommunityPosts (or equivalent) hitting split endpoints.
    - fetchContent uses Promise.all on four calls and merges into WebsiteData shape identical to current consumers.
    - Network tab shows 4 parallel requests instead of one monolithic /content on page load.
  </acceptance_criteria>
  <action>Update api and provider fetchContent. Preserve deep-merge with initialData. Handle partial failure with console.error per slice.</action>
</task>

<task type="auto">
  <name>Task 3: Thin initialData skeleton and seed documentation</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/src/lib/websiteData.ts
    - /Users/anuragverma/Downloads/book website-frontend/server/src/seed.ts
    - /Users/anuragverma/Downloads/book website-frontend/.planning/codebase/CONCERNS.md
  </read_first>
  <files>src/lib/websiteData.ts, server/src/seed.ts</files>
  <acceptance_criteria>
    - websiteData.ts file header comment contains phrase skeleton-only and states server seed is authoritative.
    - initialData articles, events, communityPosts are empty arrays []; hero/settings contain only structural keys needed before API loads (no long demo copy).
    - seed.ts top comment references BACK-06 single source of truth.
    - App still renders after API failure using skeleton without duplicate demo blog posts in bundle.
  </acceptance_criteria>
  <action>Strip demo content from initialData keeping types satisfied. Add documentation comments in websiteData.ts and seed.ts. Do not delete seed demo content.</action>
</task>

</tasks>

<verification>
1. npm run build (root) exits 0
2. Start server; curl http://localhost:3001/api/v1/content/articles?limit=2 → max 2 articles
3. Browser DevTools: 4 parallel content requests on homepage load
4. grep -q skeleton-only src/lib/websiteData.ts
5. Disconnect API: site shows empty lists not stale demo articles from bundle
</verification>

<success_criteria>
- BACK-06 and BACK-07 satisfied; ROADMAP success criterion 4 met.
</success_criteria>

<output>
After completion, create .planning/phases/01-backend-completeness/01-03-SUMMARY.md
</output>
