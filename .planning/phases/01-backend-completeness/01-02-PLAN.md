---
phase: 01-backend-completeness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/anuragverma/Downloads/book website-frontend/server/package.json
  - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/adminRoutes.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/authRoutes.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/schemas/content.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/schemas/article.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/schemas/event.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/schemas/community.ts
  - /Users/anuragverma/Downloads/book website-frontend/server/src/middleware/validateBody.ts
  - /Users/anuragverma/Downloads/book website-frontend/src/lib/api.ts
  - /Users/anuragverma/Downloads/book website-frontend/src/pages/AdminPage.tsx
requirements:
  - BACK-04
  - BACK-05
autonomous: true
must_haves:
  truths:
    - "D-11: AdminPage validates JWT on every /admin/* mount via GET /api/v1/admin/me before dashboard."
    - "D-12: /admin/me returns { ok, role, username, exp } profile snippet."
    - "D-13: Expired/invalid token redirects to login with clear message (no refresh flow)."
    - "D-14: 401 from /admin/me clears adminToken and config.admin.sessionKey."
    - "D-18: Shared Zod schemas in server/src/schemas/ validate all admin mutating routes with 400 + field errors."
  artifacts:
    - path: /Users/anuragverma/Downloads/book website-frontend/server/src/middleware/validateBody.ts
      provides: Zod validation middleware
    - path: /Users/anuragverma/Downloads/book website-frontend/server/src/schemas/
      provides: Shared DTO schemas
  key_links:
    - from: /Users/anuragverma/Downloads/book website-frontend/src/pages/AdminPage.tsx
      to: /Users/anuragverma/Downloads/book website-frontend/server/src/routes/adminRoutes.ts
      via: GET /admin/me on mount
      pattern: getAdminMe|/admin/me
---

<objective>
Add admin session validation on load and Zod-validated admin mutations.

Purpose: Deliver BACK-04 and BACK-05; close stale-token dashboard and silent Prisma failures.
Output: GET /admin/me, validateBody middleware, schemas on all admin write routes.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-backend-completeness/01-CONTEXT.md
@.planning/phases/01-backend-completeness/01-RESEARCH.md
</execution_context>

<threat_model>
| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-01-03 | Stale JWT grants admin UI access | high | /admin/me on every /admin/* mount (D-11) |
| T-01-04 | Mass-assignment corrupts Prisma records | medium | Zod strip unknown keys on admin writes |
</threat_model>

<context>
Locked: D-11–D-14, D-18. /admin/me response shape: { ok: true, role, username, exp }.
On 401: clear adminToken and config.admin.sessionKey (from src/lib/config.ts).
JWT payload today: { role: 'admin' } — include username admin from DB lookup.
No refresh tokens in Phase 1.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Zod and create validateBody middleware</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/package.json
    - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/adminRoutes.ts
  </read_first>
  <files>server/package.json, server/src/middleware/validateBody.ts</files>
  <acceptance_criteria>
    - zod listed in server/package.json dependencies.
    - validateBody.ts exports middleware returning 400 with { error: 'Validation failed', details: [{ path, message }] } on ZodError.
  </acceptance_criteria>
  <action>Add zod dependency. Create validateBody(schema) middleware using schema.safeParse and structured error details array.</action>
</task>

<task type="auto">
  <name>Task 2: Create shared Zod schemas for admin DTOs</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/adminRoutes.ts
    - /Users/anuragverma/Downloads/book website-frontend/server/prisma/schema.prisma
    - /Users/anuragverma/Downloads/book website-frontend/.planning/phases/01-backend-completeness/01-CONTEXT.md
  </read_first>
  <files>server/src/schemas/content.ts, server/src/schemas/article.ts, server/src/schemas/event.ts, server/src/schemas/community.ts</files>
  <acceptance_criteria>
    - contentPatchSchema validates optional hero, settings, appearance, stats, pillars, perks objects.
    - articleSchema and eventSchema cover fields used in blog/event create (reject unknown keys via .strict() or strip).
    - communityAdminSchemas cover admin community post create/update.
    - Schemas exported for use in adminRoutes.
  </acceptance_criteria>
  <action>Create server/src/schemas/ files matching Prisma write shapes. Use z.object with required fields for create and partial for patch where appropriate.</action>
</task>

<task type="auto">
  <name>Task 3: Add GET /admin/me and apply validation to admin routes</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/adminRoutes.ts
    - /Users/anuragverma/Downloads/book website-frontend/server/src/routes/authRoutes.ts
    - /Users/anuragverma/Downloads/book website-frontend/server/src/middleware/validateBody.ts
  </read_first>
  <files>server/src/routes/adminRoutes.ts</files>
  <acceptance_criteria>
    - GET /me returns { ok: true, role, username: 'admin', exp } from decoded JWT.
    - Invalid token on /me returns 401 JSON { error: string }.
    - PATCH /content, POST/PUT /blogs, POST/PUT /events, admin community routes use validateBody before handler.
    - Invalid blog create returns 400 with details array containing field path (e.g. title).
  </acceptance_criteria>
  <action>Register GET /me on admin router with authenticateAdmin. Wire validateBody to each mutating route. Replace raw req.body spread with parsed schema output.</action>
</task>

<task type="auto">
  <name>Task 4: AdminPage session validation on mount</name>
  <read_first>
    - /Users/anuragverma/Downloads/book website-frontend/src/pages/AdminPage.tsx
    - /Users/anuragverma/Downloads/book website-frontend/src/lib/api.ts
    - /Users/anuragverma/Downloads/book website-frontend/src/lib/config.ts
  </read_first>
  <files>src/lib/api.ts, src/pages/AdminPage.tsx</files>
  <acceptance_criteria>
    - api.getAdminMe(token) calls GET /api/v1/admin/me with Bearer header.
    - AdminPage useEffect: if adminToken exists, call getAdminMe; on 401 clear adminToken and config.admin.sessionKey, setIsAuthenticated(false), setError message about session expired.
    - While validating, show Loader2 (loading state) instead of dashboard.
  </acceptance_criteria>
  <action>Add getAdminMe to api.ts. Refactor AdminPage initial auth check to async validation before rendering Routes.</action>
</task>

</tasks>

<verification>
1. curl http://localhost:3001/api/v1/admin/me -H 'Authorization: Bearer INVALID' → 401
2. Login via UI, copy token, curl /admin/me → 200 with ok, role, username, exp
3. curl -X POST http://localhost:3001/api/v1/admin/blogs -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' -d '{}' → 400 with details
4. Browser: set garbage adminToken, open /admin/dashboard → login screen with message
</verification>

<success_criteria>
- BACK-04 and BACK-05 satisfied; ROADMAP success criteria 2 and 3 met for this plan scope.
</success_criteria>

<output>
After completion, create .planning/phases/01-backend-completeness/01-02-SUMMARY.md
</output>
